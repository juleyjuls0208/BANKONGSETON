# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | arch | Fraud alert persistence | Google Sheets "Fraud Alerts" worksheet | Consistent with all other data storage; no new infra; readable by admin | Yes — if volume demands a real DB |
| D002 | M001 | arch | Cashier accounts storage | Google Sheets "Cashier Accounts" worksheet | Same pattern as Users; admin can view/edit directly in Sheets if needed | Yes — if security requirements tighten |
| D003 | M001 | arch | Offline queue persistence | SQLite (stdlib sqlite3, WAL mode) | Survives process restart; no new package; single-process so no contention | No |
| D004 | M001 | pattern | Cashier password storage | bcrypt hash in Cashier Accounts sheet | Passwords must never be stored plaintext; bcrypt is the standard | No |
| D005 | M001 | library | SMS notifications | Twilio Python SDK | CHANGELOG referenced it; widely used in PH; gated on env vars | Yes — if another SMS gateway preferred |
| D006 | M001 | library | Daily scheduler | APScheduler BackgroundScheduler with threading.Timer fallback | APScheduler is cleaner; fallback ensures it works even without pip install | Yes — if moving to a task queue |
| D007 | M001 | pattern | Transaction void | Append new void record to Transactions Log + restore balance | Preserves audit trail; no row deletion; consistent with existing log pattern | No |
| D008 | M001 | pattern | Lost card FCM message type | New FCM data message type "card_replaced" handled in FCMService | Decoupled from notification; app can update state on receive | No |
| D009 | M001/S01 | pattern | Fraud Sheets lazy init | _get_fraud_detector_with_sheets() called on first API request, not at app startup | Avoids blocking app boot when Sheets is unavailable; acceptable latency tradeoff on first fraud-page load | Yes — if strict cold-start time SLA required |
| D010 | M001/S01 | pattern | Fraud sheet persistence non-fatal | Sheet write/read failures logged at WARNING level; in-memory FraudDetector remains source of truth | Sheets outage should not hard-block fraud detection in-process; data can be re-persisted on reconnect | No |
| D011 | M001/S01 | security | resolve_fraud_alert authorization | @login_required only (finance role can resolve); suspend/unsuspend require @admin_only | Finance staff handle day-to-day alert triage; only admins should lock/unlock cards | Yes — if tighter access control needed |
| D012 | M001/S01 | pattern | load_from_sheets consolidation | load_from_sheets(fraud_ws, suspended_ws) loads both worksheets in one call instead of two separate methods | Reduces Sheets round-trips; both datasets are always needed together on init | No |
| D056 | M006 | arch | Standalone cashier app isolation | New Flask app at backend/cashier_app/app.py on port 5010; own SocketIO instance; own session secret | Process isolation — cashier station runs independently of admin dashboard; pending_qr_token is per-process | No |
| D057 | M006 | arch | Cashier app deployment | Local/LAN only (no PythonAnywhere) | User requirement; school canteen LAN; Arduino WiFi also on LAN | No |
| D058 | M006 | pattern | Product card display | Color-coded by category — no product images | Sheets has no image URLs; color coding is instant, readable, zero-setup | Yes — if images added to Sheets later |
| D002 | M001 | arch | Cashier accounts storage | Google Sheets "Cashier Accounts" worksheet | Same pattern as Users; admin can view/edit directly in Sheets if needed | Yes — if security requirements tighten |
| D003 | M001 | arch | Offline queue persistence | SQLite (stdlib sqlite3, WAL mode) | Survives process restart; no new package; single-process so no contention | No |
| D004 | M001 | pattern | Cashier password storage | bcrypt hash in Cashier Accounts sheet | Passwords must never be stored plaintext; bcrypt is the standard | No |
| D005 | M001 | library | SMS notifications | Twilio Python SDK | CHANGELOG referenced it; widely used in PH; gated on env vars | Yes — if another SMS gateway preferred |
| D006 | M001 | library | Daily scheduler | APScheduler BackgroundScheduler with threading.Timer fallback | APScheduler is cleaner; fallback ensures it works even without pip install | Yes — if moving to a task queue |
| D007 | M001 | pattern | Transaction void | Append new void record to Transactions Log + restore balance | Preserves audit trail; no row deletion; consistent with existing log pattern | No |
| D008 | M001 | pattern | Lost card FCM message type | New FCM data message type "card_replaced" handled in FCMService | Decoupled from notification; app can update state on receive | No |
| D009 | M001/S01 | pattern | Fraud Sheets lazy init | _get_fraud_detector_with_sheets() called on first API request, not at app startup | Avoids blocking app boot when Sheets is unavailable; acceptable latency tradeoff on first fraud-page load | Yes — if strict cold-start time SLA required |
| D010 | M001/S01 | pattern | Fraud sheet persistence non-fatal | Sheet write/read failures logged at WARNING level; in-memory FraudDetector remains source of truth | Sheets outage should not hard-block fraud detection in-process; data can be re-persisted on reconnect | No |
| D011 | M001/S01 | security | resolve_fraud_alert authorization | @login_required only (finance role can resolve); suspend/unsuspend require @admin_only | Finance staff handle day-to-day alert triage; only admins should lock/unlock cards | Yes — if tighter access control needed |
| D012 | M001/S01 | pattern | load_from_sheets consolidation | load_from_sheets(fraud_ws, suspended_ws) loads both worksheets in one call instead of two separate methods | Reduces Sheets round-trips; both datasets are always needed together on init | No |
| D013 | M002 | arch | FraudDetector worker constraint | Hard-fail at startup if WEB_CONCURRENCY > 1 (not multi-worker refactor) | Module-level singleton is safe for single-worker; refactor to poll-on-request adds latency with no benefit at current PythonAnywhere scale | Yes — if scaling to multi-worker becomes necessary |
| D014 | M002 | pattern | Cache coverage scope | Wire cache.py to hot read endpoints + mutation invalidation; not full coverage | 2 cached vs 100 raw calls is the real gap; full coverage would require touching every handler and adds fragility; risk-based approach covers the quota-breaking paths | Yes — if additional endpoints become hot |
| D015 | M002 | pattern | Test coverage bar | Critical money-moving paths (~35 tests) not broad metric-based coverage | admin_dashboard.py + cashier_routes.py are ~3000 lines; broad coverage would take 3-4x longer and be fragile to refactor; complete_sale/load_balance/void/auth are where real production risk lives | Yes — if broader coverage is warranted post-deployment |
| D016 | M002/S01 | ops | api requirements self-contained | Each Flask app owns its full dependency list independently — no cross-app inheritance | api_server.py is deployed independently on PythonAnywhere; relying on dashboard's venv for gspread/firebase-admin/twilio was silent deploy-time failure | No |
| D017 | M002/S02 | pattern | Cache import fallback in cashier_routes.py | Import cache with try/except; provide no-op lambda fallbacks | cashier_routes.py is three levels deep; import failure should degrade gracefully (Sheets still works), not crash the cashier POS | Yes — remove fallback if import reliability is confirmed |
| D018 | M002/S02 | pattern | Balance reads in payment flows uncached | Money Accounts reads inside complete_sale, process_cashier_transaction, and nfc_pay always hit Sheets directly | Stale cached balance could allow overdraft; 30s TTL is too coarse for payment safety; display reads (get_profile, get_balance, get_students) are lower risk | No |
| D019 | M002/S03 | pattern | api_server.py health uses fresh Sheets client per request | get_sheets_client() called fresh inside health_check(), not reusing stale module-level db | Stale client state can mask live connectivity failures; health check must reflect current reachability, not startup-time connection success | No |
| D020 | M002/S03 | pattern | latency_ms=0 as not-initialized sentinel | When db is None, latency_ms is set to 0 (not computed) in dashboard health handlers | Distinguishes "client never initialized" (bad env/creds) from "extremely fast response"; critical for ops diagnosis from the health JSON alone | No |
| D021 | M002/S03 | pattern | _parse_worker_count helper for safe env var parsing | try/except int() with fallback to 1 on empty string or ValueError | PythonAnywhere may set WEB_CONCURRENCY= (empty); guard must not false-positive or crash on empty/non-numeric values | No |
| D022 | M002/S04 | pattern | cashier blueprint sys.path registration in conftest | Add `backend/dashboard/` to sys.path in conftest.py flask_app fixture before admin_dashboard import | cashier blueprint uses `from cashier.cashier_routes import cashier_bp` at admin_dashboard module level; without `backend/dashboard/` in sys.path this fails silently, leaving routes unregistered and all cashier tests returning 404 | No |
| D023 | M002/S04 | bugfix | transaction_row pre-built before retry loop | Moved transaction_row construction before the retry loop in complete_sale | Original code built it inside the loop after update_cell; UnboundLocalError in the offline fallback when update_cell always fails; test exposed this production bug | No |
| D024 | M002/S04 | pattern | email/notifications patched via sys.modules injection | Use `patch.dict('sys.modules', {})` for email_service and notifications | Both imported via inline sys.path.insert + from X import Y; sys.modules injection intercepts before path search, making tests independent of whether backend/services/ is in sys.path at test time | Yes — switch to direct patch if sys.path guaranteed |
| D025 | M002/S04 | pattern | patch.object with create=True for PHASE3_AVAILABLE-gated names | `patch.object(adm_module, 'get_sms_notifier', create=True)` and `patch.object(adm_module, 'invalidate_pattern', create=True)` | PHASE3_AVAILABLE is True in test env (all backend/ modules importable) so these attributes exist; create=True makes the patch safe in environments where they don't exist | No — always use create=True for optional module attributes |
| D026 | M002/S04 | testing | admin_only decorator returns 403 for finance role | Test asserts `response.status_code != 200` rather than specific 403/401/302 | Spec said "401 or 302"; actual admin_only returns 403 for authenticated-but-wrong-role; asserting != 200 is robust to future decorator implementation changes while still proving access is denied | Yes — tighten to assert 403 if decorator behavior is stabilized |
| D027 | M002/S05 | ops | DEPLOY.md written from scratch, not adapted from existing docs | New file at docs/DEPLOY.md | DEPLOYMENT_PYTHONANYHERE.md and DEPLOYMENT_GUIDE.md had stale paths, wrong WSGI project_home depth, and predated M002 startup guards; adapting would have required more effort than writing fresh | Yes — if a separate ops-team-facing guide is ever needed |
| D028 | M003 | arch | Arduino WiFi routing split by prefix | Two separate httpPost helpers (httpPostCard, httpPostNFC) dispatched by prefix parameter in deliver() | Backend already has two distinct endpoints with different payloads (uid vs token) and different SocketIO events (card_read vs nfc_payment); single-endpoint approach would require backend changes; firmware-only fix is safer | No |
| D029 | M003 | arch | Arduino heartbeat as dual-purpose keep-alive | Single POST /api/arduino/heartbeat every 30s serves both WiFi status (cashier UI badge) and powerbank keep-alive (sustained current draw) | Avoids separate keep-alive mechanism; heartbeat POST triggers RF burst that keeps powerbank above auto-shutoff threshold; 30s is short enough to detect Arduino going offline quickly | Yes — if 30s polling is too chatty for school LAN |
| D030 | M003 | pattern | Phone NFC token resolution in cashier_routes.py (not cross-process call to api_server.py) | complete_sale_nfc() in cashier_routes.py does its own VirtualCards sheet lookup | api_server.py nfc_pay() requires cashier JWT auth but runs in a separate process; HTTP call from cashier to API adds a round-trip and network dependency; direct Sheets lookup in cashier_routes is consistent with how complete_sale() works | Yes — if token validation logic must be centralized |
| D031 | M003 | pattern | arduinoConnected honors both serial and WiFi states | Existing serial connect path unchanged; WiFi path sets same arduinoConnected=true flag via arduino_wifi_status socket event | Keeps both modes working simultaneously; cashier who has both a serial cable and WiFi Arduino sees no change; cashier with WiFi-only Arduino gets full checkout functionality | No |
| D032 | M003/S02 | pattern | VirtualCards MoneyCardNumber matched by direct string — no normalize_card_uid | complete_sale_nfc() uses direct string comparison for MoneyCardNumber against Money Accounts rows | VirtualCards sheet stores the canonical card number (inserted at registration time); normalization would be wrong and could produce false mismatches; physical card path uses normalize_card_uid() only because raw RFID UIDs have byte-order variants | No |
| D033 | M003/S02 | pattern | Inbound socket event handlers do not gate on arduinoConnected | completeNFCSale and card_read handlers call their fetch functions unconditionally; no arduinoConnected check | Connection-state gating is only meaningful for outbound actions (initiating a read); by the time nfc_payment fires, the tap already arrived — gating would silently drop a real payment event | No |
| D034 | M003/S03 | pattern | arduino_last_heartbeat initialized to 0.0 float; last_seen_s = -1.0 sentinel when never heartbeated | app.arduino_last_heartbeat = 0.0 at startup; status route returns last_seen_s = -1.0 when last_hb == 0 | Float init means arithmetic is always safe (no None-guard); -1.0 sentinel is clearly distinguishable from a real elapsed time and from zero-as-elapsed-zero; consumers should use the `online` field as the authoritative signal | No |
| D035 | M003/S03 | pattern | WiFi offline does not fight serial connection in updateWifiBadge() | updateWifiBadge() checks serial statusIndicator class before clearing arduinoConnected on WiFi offline | Serial connection is the stronger signal — if the cashier has a USB cable plugged in, a WiFi dropout should not disable the Pay Now button; WiFi path only clears arduinoConnected when serial is also not connected | No |
| D036 | M003/S04 | pattern | Heartbeat timer block placed before NFC early-exit in loop() | 4-line heartbeat block inserted after handleIncomingSerial() and before if (!found) return at line 530 | Heartbeat must fire during idle periods when no card is in range; placement after the early-exit would prevent powerbank keep-alive and badge updates during quiet periods between taps | No |
| D037 | M004/S01 | pattern | APDU inDataExchange wrapped in retry loop | Up to APDU_MAX_RETRIES=3 attempts; APDU_RETRY_DELAY_MS=150ms between attempts; responseLength reset to 60 before each; break on first success | Single inDataExchange call fails when Android HCE service isn't fully initialized after RF field cycle; ok=NO rspLen=60 confirmed in Serial Monitor; retry handles phone-to-phone HCE startup latency variation without over-penalizing fast phones with a fixed long delay | Yes — increase APDU_MAX_RETRIES or APDU_RETRY_DELAY_MS if a specific phone model still fails |
| D038 | M004/S02 | pattern | complete_sale_nfc Money Accounts lookup uses direct string comparison (aligning to D032) | Change normalize_card_uid() comparison to str(...).strip() == money_card_number | D032 decision was direct string; implementation in S02 deviated to normalize; symmetric normalization should work but aligning to D032 and api_server.py nfc_pay() reference removes the deviation and any edge-case risk | No |
| D039 | M004 | pattern | APDU exchange removed; phone payments use hardware UID via VirtualCards.PhoneUID lookup | Remove APDU block from firmware — all 4-byte cards deliver CARD|uid regardless of SAK; complete_sale() falls back to VirtualCards.PhoneUID when UID not in Money Accounts; admin registers phone UIDs via POST /api/phone/register-uid | requireDeviceUnlock="true" + isPaymentAuthorized gate in Android HCE service mean the HCE only responds when screen is unlocked AND student has done biometric auth in the app — too much friction for a canteen queue; rspLen=60 (zero bytes) confirmed on real tap even with 3 retries; phone hardware UID is stable for a given device (0x08-prefix random-but-fixed-at-first-use); UID approach has zero tap friction | Yes — if Android app removes requireDeviceUnlock and auth gate, APDU can be re-added as a first-try with UID fallback |
| D040 | M004 | pattern | Re-enable APDU token path; keep UID fallback auxiliary | Restore firmware APDU retry loop (D037) and set Android HCE requireDeviceUnlock=false + token-only APDU response (remove biometric gate in handleSelectCommand); retain VirtualCards.PhoneUID path as optional fallback | D039 invalidated by runtime evidence: same phone produced different 0x08-prefixed UIDs (0869016D then 08E13C89), so hardware UID is not a stable primary key; token APDU is the durable identifier across taps/devices. HCE silence root cause was policy gates, not AID mismatch. | Yes — if future Android policy changes require reintroducing stricter in-app auth gates with acceptable cashier UX |
| D041 | M005 | arch | PN532 → RC522 on R4; phone NFC/HCE payment retired entirely | RC522 RFID-only on both Arduinos; QR code payment replaces phone NFC | PN532 APDU timing issues (M003/M004) are a hardware-level liability; RC522 is already proven on R3 and handles all payment-relevant cards; QR payment is simpler, works on Android + iOS, and has no RF proximity requirement | No |
| D042 | M005 | arch | LCD → SSD1306 OLED on R4; LCD removed entirely | Adafruit SSD1306 (128×64) + Adafruit GFX via hardware I2C (Wire.h) on SDA/SCL | 16×2 character LCD cannot render a QR bitmap; OLED is mandatory for QR display; Adafruit SSD1306 + GFX is the standard Arduino OLED stack with drawPixel() support needed for QR rendering | No |
| D043 | M005 | arch | QR delivery via Arduino polling; no push from server | Arduino polls GET /api/arduino/qr-pending every ~500ms; backend stores pending token in app.pending_qr_token (in-memory) | Consistent with existing heartbeat/WiFi pattern; avoids Arduino running an HTTP server; ~500ms display lag is acceptable; in-memory storage is fine for single-worker PythonAnywhere; token expires after 5 minutes if unclaimed | Yes — switch to WebSocket push if display latency becomes a problem |
| D044 | M005 | arch | QR encodes full URL; app fetches cart separately | QR bitmap encodes https://<SERVER_URL>/api/qr/<token>; app calls GET /api/qr/<token> to fetch cart before showing confirmation | URL-encoded QR works with both Android ML Kit and iOS AVFoundation scanners without custom parsing; cart details are fetched server-side so token stays opaque | No |
| D045 | M005 | library | Android QR scanning via ML Kit Barcode Scanning | com.google.mlkit:barcode-scanning — firebase-bom already in deps | ML Kit is the modern Android choice; already in firebase-bom; CameraX for preview | Yes — switch to ZXing if ML Kit causes build issues |
| D046 | M005 | library | iOS QR scanning via AVFoundation (no new dependency) | AVCaptureMetadataOutput with AVMetadataObjectTypeQRCode | Built-in; no new package; standard SwiftUI approach via UIViewControllerRepresentable | No |
| D047 | M005 | library | Arduino QR generation via qrcode by Richard Moore | github.com/ricmoo/qrcode — generates raw QR bit matrix; render with Adafruit GFX drawPixel() | Lightweight, no dynamic allocation, works on UNO-class RAM | Yes — if RAM pressure forces a different approach |
| D048 | M005/S02 | pattern | display.display() called once per renderQr(), outside pixel loop | Single display.display() call after all drawPixel() calls complete | Calling display.display() inside the pixel loop would flood the I2C bus with 128×64 partial-frame transfers; one flush after all pixels are set minimises I2C traffic and eliminates frame-tear artifacts | No |
| D049 | M005/S02 | pattern | Non-fatal peripheral init for OLED | OLED init failure logs Serial message and continues; firmware does not halt | RFID payment is the critical function; OLED is display-only; halting on OLED failure would break the cashier terminal over a non-critical peripheral fault | No |
| D050 | M005/S02 | pattern | QR poll guard on lastQrUrl equality check | OLED only redraws when qrUrl != lastQrUrl — unchanged state skips clearDisplay+pixel loop | Without the guard, every 500ms poll would re-render identical pixels, causing continuous I2C traffic and visible flicker; guard makes QR display state-driven not timer-driven | No |
| D051 | M005/S03 | pattern | import jwt as _pyjwt in web_app.py | Alias to _pyjwt to avoid naming collision with any local 'jwt' variable; api_server.py keeps bare 'jwt' | web_app.py has multiple routes that use a local variable named 'jwt'; bare import would shadow it; api_server.py is clean so it keeps bare import | No |
| D052 | M005/S03 | pattern | current_app.pending_qr_token in cashier blueprint (not bare app.) | cashier_routes.py writes pending_qr_token via current_app proxy; web_app.py reads directly from app | Blueprint runs inside Flask application context; bare app.pending_qr_token would be RuntimeError outside context; current_app proxy is the correct pattern for blueprint-to-app state writes | No |
| D053 | M005/S03 | pattern | socketio.emit('qr_payment') fires before app.pending_qr_token = None | Emit precedes token clear in all qr_confirm code paths | Prevents timing race where OLED polls qr-pending and receives null before cashier UI has processed the qr_payment event; emit-then-clear is the safe ordering | No |
| D054 | M005/S03 | pattern | 402 for QR insufficient funds (not 400) | HTTP 402 returned when student balance < cart total in qr_confirm | Distinguishes insufficient-funds from bad-request (400); S04 mobile apps can branch on status code to show a specific "insufficient balance" message rather than a generic error dialog | Yes — align to whatever HTTP convention the iOS/Android apps settle on |
| D055 | M005/S03 | pattern | app.pending_qr_token is in-memory only; single token system-wide | Single pending_qr_token dict in Flask app object; resets on process restart; second cashier qr-generate overwrites first | Single-worker PythonAnywhere deployment; no concurrency issue; multi-station would need per-session token scoping; documented in .env.example and DEPLOY.md | Yes — if multi-cashier concurrent deployment is ever needed |
| D059 | M006/S02 | pattern | Cart row rendering in POS template | Build cart DOM rows with `document.createElement` + `textContent` (not string `innerHTML`) for product names/prices from Sheets | Product data is operator-editable in Google Sheets; DOM API rendering prevents accidental HTML/script injection and keeps quantity control attributes explicit for automation/tests | No |
| D060 | M006/S02 | pattern | POS fetch auth failure handling | Treat both explicit 401 and redirected `/login` fetch responses as unauthenticated and hard-redirect to `/login` | Standalone cashier auth currently redirects unauthenticated API calls; checking both signals keeps frontend robust if backend later switches between redirect vs direct 401 semantics | No |
| D061 | M006/S03 | pattern | Arduino WiFi status route naming in standalone app | Serve `/api/arduino/wifi-status` as canonical route and keep `/api/arduino-wifi-status` as compatibility alias with identical payload | M006 roadmap uses slash-form route while prior implementations used hyphen-form; canonical+alias removes ambiguity and prevents firmware/UI breakage during migration to port 5010 | Yes — alias can be removed once all clients consistently use canonical route |
| D062 | M006/S03 | pattern | NFC scope for standalone cashier payment | Keep `/api/complete-sale-nfc` as a supported standalone compatibility path, but do not introduce new firmware/mobile NFC mechanisms in this slice | Roadmap requires NFC-capable sale completion in standalone app, while M005 shifted primary hardware flow to RFID+QR; preserving existing backend NFC contract satisfies checkout compatibility without reopening retired hardware scope | Yes — revisit if product direction fully removes or re-expands NFC |
| D063 | M006/S03 | pattern | Standalone money-route error contract | Return HTTP 402 + `balance`/`required` for insufficient funds and include `queue` metadata when falling back to offline sync | Cashier UI and future agents must distinguish operator-correctable insufficient funds from backend outages; queue metadata exposes offline state without tailing logs | Yes — adjust only if frontend error contract changes |
| D064 | M006/S03 | pattern | Standalone POS payment lifecycle orchestration | Use a frontend payment state machine that always starts with `/api/process-sale`, then branches to RFID (`card_read` → `/api/complete-sale`), QR (`/api/qr-generate` + `qr_payment` event), or NFC (`/api/complete-sale-nfc`), with `/api/cancel-sale` and queue/wifi diagnostics always available | Keeps cashier-visible states deterministic (in-progress, waiting, success, cancelled, failed), preserves S02 cart architecture, and centralizes auth-failure redirect handling via one request helper instead of ad-hoc endpoint checks | Yes — revisit if backend payment contracts change |
| D065 | M006/S03 | pattern | Standalone payment/QR routes use lazy helper imports | Import dashboard payment helper functions inside route handlers (not module top-level) in `backend/cashier_app/routes/payment.py` and `backend/cashier_app/routes/arduino.py` | Avoids admin-dashboard import side effects during standalone app startup/tests while still reusing battle-tested money logic; improves patchability in route-contract tests | Yes — can be revisited if helper modules are later extracted into side-effect-free shared package |
| D066 | M006/S04 | pattern | Live proof closure requires verifier artifact with offline classification | Use a deterministic S04 verifier that writes machine-readable evidence (`S04-LIVE-PROOF.json`) and treats `success:true` + `offline:true` as degraded (not closure) for `/api/products`, `/api/complete-sale`, `/api/qr/confirm`, `/api/complete-sale-nfc` | S03 contract/runtime checks proved wiring, but milestone closure remained ambiguous without non-mocked live-Sheets proof; explicit artifact + classification prevents false positives and gives repeatable audit evidence | Yes — thresholds/required flows can be adjusted if payment contracts change |
| D067 | M006/S04 | pattern | Live verifier endpoint probing strategy | Probe canonical `/api/*` routes first, then fallback to `/cashier/api/*` equivalents while keeping canonical endpoint names in persisted evidence | Current runtime exposes cashier flows under `/cashier/api/*` while closure contracts reference `/api/*`; candidate probing keeps runtime checks real without losing contract alignment | Yes — remove fallback once route prefixes are fully unified |
| D068 | M006/S05 | pattern | Physical UAT closure bundle for standalone cashier | Gate slice closure on a combined S05 evidence bundle that links S04 live-proof output with timestamped physical artifacts (RFID tap, QR confirm, heartbeat, NFC-compatible path) and request traces on `:5010` | S05 is operator/hardware proof, not new payment logic; combining machine evidence + human captures prevents false closure when routes pass but physical flow or topology assumptions fail | Yes — adjust artifact checklist if hardware/payment contracts evolve |
| D069 | M006/S05 | observability | S05 verifier required-flow fusion and artifact gate | Compute each required S05 flow from S04 required-phase classification + physical-check evidence, and force failure when artifact references or request-trace endpoint hits are missing | Prevents false positives where API calls pass but physical proof is absent; surfaces exact failure reasons in `overall.failure_reasons`, `physical_checks.*.failure_reasons`, and `artifacts.missing_references` for auditable closure diagnostics | Yes — revisit if required physical checks or endpoint contracts change |
