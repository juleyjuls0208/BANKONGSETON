---
phase: 32.1-nfc-fix
plan: "02"
subsystem: firmware
tags: [arduino, nfc, pn532, ntag215, serial]

# Dependency graph
requires:
  - phase: 32.1-nfc-fix
    provides: Phase context and R4 reference patterns to backport to R3

provides:
  - RF field toggle (setRFField 0,0→0,1) before each scan in R3 loop()
  - UID-length dispatch: NTAG215 (uidLen != 4) emits CARD|<uid> directly, skips APDU
  - Diagnostic print of pn532_packetbuffer on no-card events in R3

affects: [32.1-nfc-fix, arduino-bridge, nfc-scanning]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "RF field toggle before readPassiveTargetID for NTAG215 passive wake-up"
    - "UID-length dispatch: 4-byte UID = HCE phone (APDU path), != 4 = physical card (CARD| path)"

key-files:
  created: []
  modified:
    - arduino/bankongseton_nfc_r3/bankongseton_nfc_r3.ino

key-decisions:
  - "uidLen != 4 means physical card (NTAG215 = 7-byte), == 4 means HCE phone — dispatch on this invariant"
  - "RF toggle cycle (off→on) required before each scan; NTAG215 never powers up without it"
  - "Diagnostic print added to !found branch so no-detection events are visible on serial"

patterns-established:
  - "RF toggle pattern: setRFField(0,0)/delay(50)/setRFField(0,1)/delay(10) before every scan"
  - "UID dispatch pattern: if (uidLen != 4) → CARD|uid; else → APDU path"

requirements-completed:
  - NFC-FIX-02

# Metrics
duration: ~10min
completed: 2026-03-11
---

# Phase 32.1-nfc-fix Plan 02: R3 RF Toggle + UID-Length Dispatch Summary

**RF field toggle, UID-length dispatch, and pn532_packetbuffer diagnostic print backported from R4 into R3 sketch so NTAG215 physical cards are detected and reported via `CARD|<uid>` serial protocol**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-03-11T00:00:00Z
- **Completed:** 2026-03-11T00:10:00Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- RF field cycle (off→on) inserted before `readPassiveTargetID` — NTAG215 now powers up reliably
- `uidLen != 4` branch added: NTAG215 cards (7-byte UID) emit `CARD|<uid>` and bypass APDU path entirely
- `pn532_packetbuffer` hex dump added to the `!found` branch for serial-level diagnostics
- APDU SELECT AID path for 4-byte UID (HCE phones) left completely unchanged

## Task Commits

Each task was committed atomically:

1. **Task 1: Add RF toggle + diagnostic print to R3 loop()** - `90aaf4c` (feat)
2. **Task 2: Add UID-length dispatch to R3 loop()** - `8982e23` (feat)

**Plan metadata:** _(docs commit follows)_

## Files Created/Modified
- `arduino/bankongseton_nfc_r3/bankongseton_nfc_r3.ino` — Added RF toggle before scan, `!found` diagnostic print, and `uidLen != 4` dispatch block wrapping the existing APDU path

## Decisions Made
- Used `uidLen != 4` as the dispatch invariant (NTAG215 = 7 bytes, HCE phone = 4 bytes) — matches R4 reference implementation
- `delay(SCAN_COOLDOWN_MS)` left outside both branches, at the end of `loop()`, unchanged
- No WiFi/HTTP code added; baud rate remains 9600

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- R3 sketch now has feature parity with R4 for NTAG215 physical card detection
- ArduinoBridge should receive `CARD|<uid>` from R3 for physical taps, same format as R4
- Ready to test with physical NTAG215 card against R3 hardware

---
*Phase: 32.1-nfc-fix*
*Completed: 2026-03-11*
