---
phase: 32.1-nfc-fix
plan: "03"
subsystem: firmware/bridge
tags: [arduino, nfc, python, arduino-bridge, serial-protocol]

# Dependency graph
requires:
  - phase: 32.1-nfc-fix
    plan: "01"
    provides: R4 RF field fix (setRFField 0,1)
  - phase: 32.1-nfc-fix
    plan: "02"
    provides: R3 RF toggle + UID-length dispatch

provides:
  - Full ArduinoBridge rewrite with _parse_line, _start_serial_thread, start_background_listener
  - NFC|/CARD|/ERROR| pipe-delimited protocol support
  - Background serial listener thread started from constructor

affects: [32.1-nfc-fix, arduino-bridge, nfc-scanning, dashboard-core]

# Tech tracking
tech-stack:
  added:
    - requests (already present; now used in ArduinoBridge._post_nfc_payment)
  patterns:
    - "Background serial reader thread started in constructor (_start_serial_thread named method for testability)"
    - "Pipe-delimited wire protocol: NFC|/CARD|/ERROR| prefix dispatch in _parse_line"
    - "_timeout_watcher extracted from inline loop into dedicated daemon thread"

key-files:
  created: []
  modified:
    - backend/dashboard/arduino_bridge.py

key-decisions:
  - "Named _start_serial_thread method (not inlined) so tests can patch it via patch.object"
  - "start_background_listener() is no-op if thread alive; restarts if dead — safe for repeated calls from dashboard_core.py"
  - "Break on any serial exception in _serial_loop — bridge reconnect handled externally by dashboard_core.py"

patterns-established:
  - "Background serial thread pattern: daemon thread started in __init__ via named method"
  - "_parse_line dispatch: prefix-based (NFC|/CARD|/ERROR|), no angle brackets"

requirements-completed:
  - NFC-FIX-03

# Metrics
duration: ~5min
completed: 2026-03-11
---

# Phase 32.1-nfc-fix Plan 03: ArduinoBridge Full Rewrite Summary

**Full ArduinoBridge rewrite replacing obsolete `<CARD|...>` angle-bracket parser with pipe-delimited NFC|/CARD|/ERROR| protocol, adding background serial listener thread, and all methods required by dashboard_core.py and test_arduino_bridge_nfc.py**

## Performance

- **Duration:** ~5 min
- **Completed:** 2026-03-11
- **Tasks:** 2 (read + rewrite)
- **Files modified:** 1

## Accomplishments

- `_start_serial_thread` added as a named method — allows `patch.object` in tests
- `start_background_listener()` added — resolves `AttributeError` crash in dashboard_core.py line 357
- `_serial_loop` runs as daemon thread, readline loop dispatches via `_parse_line`
- `_parse_line` handles `NFC|`, `CARD|`, `ERROR|` pipe-delimited formats
- Old `<CARD|...>` angle-bracket format completely removed
- `read_card_with_timeout` / `cancel_reading` preserved; `_timeout_watcher` extracted from inline loop
- `_post_nfc_payment` uses `CASHIER_JWT` and `API_BASE_URL` env vars with graceful no-JWT warning

## Task Commits

1. **Task 1: Read current arduino_bridge.py** — read-only, no commit
2. **Task 2: Rewrite arduino_bridge.py with full interface** — `4f4534d` (feat)

## Files Created/Modified

- `backend/dashboard/arduino_bridge.py` — Complete rewrite: 91 lines → 118 lines, all 8 required methods implemented

## Decisions Made

- Named `_start_serial_thread` method retained for test patchability (required by `test_arduino_bridge_nfc.py`)
- `start_background_listener()` is idempotent — safe for repeated calls from dashboard_core.py reconnect flow
- `break` on any exception in `_serial_loop` — external reconnect logic in dashboard_core.py handles re-initialization

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

- Set `CASHIER_JWT` env var to enable `_post_nfc_payment` to forward NFC tokens to the API server
- Set `API_BASE_URL` if API server is not at `http://127.0.0.1:5001` (default)

## Next Phase Readiness

- Plan 04 can now run: ArduinoBridge is fully functional, tests pass
- dashboard_core.py `start_background_listener()` call no longer crashes

---
*Phase: 32.1-nfc-fix*
*Completed: 2026-03-11*
