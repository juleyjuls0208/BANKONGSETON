---
phase: 32.1-nfc-fix
plan: "04"
subsystem: backend/cashier
tags: [python, cashier, uid-validation, ntag215, regex]

# Dependency graph
requires:
  - phase: 32.1-nfc-fix
    plan: "03"
    provides: ArduinoBridge emitting CARD|<uid> for 7-byte NTAG215 UIDs

provides:
  - cashier_routes.py UID_PATTERN accepting both 8-char (4-byte) and 14-char (7-byte) UIDs
  - End-to-end physical card payment path: firmware → bridge → cashier route all accept NTAG215

affects: [32.1-nfc-fix, cashier-routes, uid-validation]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "UID_PATTERN: alternation regex {8}|{14} matching both Mifare/HCE (4-byte) and NTAG215 (7-byte) UIDs"

key-files:
  created: []
  modified:
    - backend/dashboard/cashier/cashier_routes.py

key-decisions:
  - "Alternation r'^[0-9A-Fa-f]{8}$|^[0-9A-Fa-f]{14}$' matches dashboard_core.py existing pattern — backported for consistency"
  - "No other length checks found in cashier_routes.py — one-line change is sufficient"

requirements-completed:
  - NFC-FIX-04

# Metrics
duration: ~3min
completed: 2026-03-11
---

# Phase 32.1-nfc-fix Plan 04: cashier_routes.py UID Validation Fix Summary

**One-line regex change in cashier_routes.py widens UID_PATTERN from 8-char only to 8-char or 14-char, completing the end-to-end NTAG215 physical card payment path**

## Performance

- **Duration:** ~3 min
- **Completed:** 2026-03-11
- **Tasks:** 2 (fix + regression test)
- **Files modified:** 1

## Accomplishments

- `UID_PATTERN` updated from `r'^[0-9A-Fa-f]{8}$'` to `r'^[0-9A-Fa-f]{8}$|^[0-9A-Fa-f]{14}$'`
- NTAG215 7-byte UIDs (14 hex chars) now accepted at the cashier payment endpoint
- Existing 4-byte UIDs (8 hex chars, HCE phones + Mifare 1K) still accepted
- 4 NFC bridge tests pass — no regressions from Plans 01-03

## Task Commits

1. **Task 1: Fix UID_PATTERN in cashier_routes.py** — `b947d02` (fix)
2. **Task 2: Run NFC bridge tests** — verification only, no separate commit

## Files Created/Modified

- `backend/dashboard/cashier/cashier_routes.py` — Line 26: UID_PATTERN regex widened

## Decisions Made

- Used alternation `{8}|{14}` pattern — identical to dashboard_core.py pattern already deployed
- No other hard-coded length checks found in cashier_routes.py (confirmed via grep)

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None.

## Phase Complete

All 4 plans in phase 32.1-nfc-fix are now complete:

| Plan | Change | Commit |
|------|--------|--------|
| 01 | R4 setRFField(0,1) + remove setPassiveActivationRetries | 9bacf54, efd663f |
| 02 | R3 RF toggle + UID-length dispatch | 90aaf4c, 8982e23 |
| 03 | ArduinoBridge full rewrite (NFC|/CARD|/ERROR| protocol) | 4f4534d |
| 04 | cashier_routes.py UID_PATTERN accept 14-char UIDs | b947d02 |

NTAG215 physical student cards can now be detected by both R3 and R4 firmware, forwarded via the bridge as `CARD|<14-hex-uid>`, and accepted by the cashier route for payment processing.

---
*Phase: 32.1-nfc-fix*
*Completed: 2026-03-11*
