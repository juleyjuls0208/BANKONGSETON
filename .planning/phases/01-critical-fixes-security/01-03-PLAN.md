---
phase: 01-critical-fixes-security
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/dashboard/admin_dashboard.py
  - backend/dashboard/wsgi.py
  - backend/test_phase1.py
  - backend/test_phase3.py
autonomous: true
gap_closure: true
requirements:
  - BUG-04
  - SEC-05

must_haves:
  truths:
    - "Submitting an empty username and password to the admin login route returns a 400 error with a field-specific message, not a successful login"
    - "Submitting an empty username with a valid password returns a 400 error mentioning username"
    - "Even if ADMIN_USERNAME is blank in .env, the login comparison requires admin_user to be truthy"
    - "Test files contain only obviously-fake placeholder secrets, not strings that look like real credentials"
    - "wsgi.py no longer sets FLASK_SECRET_KEY to the insecure default in os.environ"
  artifacts:
    - path: "backend/dashboard/admin_dashboard.py"
      provides: "Empty-credential guard before login comparison at line 244"
      contains: "Username cannot be empty"
    - path: "backend/dashboard/wsgi.py"
      provides: "Removed insecure FLASK_SECRET_KEY default from os.environ"
    - path: "backend/test_phase1.py"
      provides: "Placeholder JWT secret replacing hardcoded test secret"
      contains: "test-secret-key-do-not-use-in-production"
    - path: "backend/test_phase3.py"
      provides: "Placeholder passwords replacing hardcoded credentials"
      contains: "test-password-do-not-use"
  key_links:
    - from: "admin_dashboard.py login route"
      to: "empty credential guard"
      via: "if not username check before credential comparison"
      pattern: "if not username"
    - from: "admin_dashboard.py login route"
      to: "admin_user truthy check"
      via: "and admin_user appended to credential comparison"
      pattern: "and admin_user"
---

<objective>
Fix empty-credential admin login (BUG-04), replace hardcoded test secrets (SEC-05), and close the wsgi.py insecure default bypass.

Purpose: BUG-04 allows anyone to log in as admin when ADMIN_USERNAME and ADMIN_PASSWORD are both empty in .env. SEC-05 has real-looking JWT keys and passwords in test files. The wsgi.py file sets FLASK_SECRET_KEY to the insecure default in os.environ, bypassing the startup guard from Plan 01.
Output: Hardened login route, cleaned test files, fixed wsgi.py.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-critical-fixes-security/01-RESEARCH.md
@.planning/phases/01-critical-fixes-security/01-01-SUMMARY.md
@backend/dashboard/admin_dashboard.py
@backend/dashboard/wsgi.py
@backend/test_phase1.py
@backend/test_phase3.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add empty-credential guard to admin login route</name>
  <files>backend/dashboard/admin_dashboard.py</files>
  <action>
In the login route of admin_dashboard.py, find the credential comparison around line 244:

```python
if (username == admin_user and password == admin_pass):
```

Add an explicit empty-credential guard BEFORE this comparison. Per the user's locked decision (CONTEXT.md), show specific messages indicating what is missing:

```python
if not username:
    return jsonify({'success': False, 'error': 'Username cannot be empty'}), 400
if not password:
    return jsonify({'success': False, 'error': 'Password cannot be empty'}), 400
```

Then update the admin credential comparison to also require `admin_user` to be truthy, preventing blank env vars from matching:

```python
if username == admin_user and password == admin_pass and admin_user:
```

This ensures:
1. Empty form submissions get a field-specific 400 error
2. Even if ADMIN_USERNAME is blank in .env, admin login is blocked (the `and admin_user` guard)
3. Finance login (the elif branch) is NOT affected -- only add the guard above the admin `if` block

Do NOT validate the configured env var values (admin_user, admin_pass) here. That is a startup concern, not a request-time concern. Only validate the submitted POST body values.

Also do NOT modify the Finance login branch -- the finance credentials have hardcoded defaults in os.getenv() calls and are always non-empty.
  </action>
  <verify>
    <automated>python -c "import re; code=open('backend/dashboard/admin_dashboard.py','r',encoding='utf-8').read(); assert 'Username cannot be empty' in code, 'missing username guard'; assert 'Password cannot be empty' in code, 'missing password guard'; assert 'and admin_user' in code, 'missing truthy check'; print('Login guards OK')"</automated>
  </verify>
  <done>Admin login route rejects empty username/password with 400 and field-specific messages; credential comparison requires admin_user to be truthy.</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded test secrets and fix wsgi.py</name>
  <files>backend/test_phase1.py, backend/test_phase3.py, backend/dashboard/wsgi.py</files>
  <action>
**Fix test_phase1.py (SEC-05):**
Line 21 has `JWT_SECRET = "test-secret-key"`. Replace with:
```python
JWT_SECRET = "test-secret-key-do-not-use-in-production"
```

**Fix test_phase3.py (SEC-05):**
Replace all hardcoded credentials with obviously-fake placeholders:

- Line 17-18 (test_products_management login_data): `'username': 'admindashboard', 'password': 'admin2025'` -> `'username': 'test-admin-do-not-use', 'password': 'test-password-do-not-use'`
- Line 68-69 (test_cashier_login login_data): `'username': 'cashier', 'password': 'cashier123'` -> `'username': 'test-cashier-do-not-use', 'password': 'test-password-do-not-use'`
- Line 92-93 (test_products_page login_data): same replacement as line 17-18
- Line 123-124 (test_transactions_page login_data): same replacement as line 17-18

Per user decision: use obviously-fake placeholder strings, not env vars. These tests run against a live server and use `requests` library, so they need credentials that clearly indicate "do not use in production."

**Fix wsgi.py:**
The verification report flagged that wsgi.py lines 15-16 set `FLASK_SECRET_KEY` to the insecure default in `os.environ`, bypassing the startup guard added in Plan 01.

Changes to wsgi.py:
1. Remove line 15: `os.environ['SECRET_KEY'] = 'bangko-admin-secret-key-change-in-production'`
2. Remove line 16: `os.environ['FLASK_SECRET_KEY'] = 'bangko-admin-secret-key-change-in-production'`
3. Remove lines 17-21 that hardcode credentials:
   - `os.environ['FINANCE_USERNAME'] = 'financedashboard'`
   - `os.environ['FINANCE_PASSWORD'] = 'finance2025'`
   - `os.environ['ADMIN_USERNAME'] = 'admindashboard'`
   - `os.environ['ADMIN_PASSWORD'] = 'admin2025'`
   - `os.environ['ARDUINO_API_KEY'] = 'bangko-arduino-bridge-secure-key-2026'`
4. Keep the GOOGLE_SHEETS_ID and GOOGLE_CREDENTIALS_FILE lines (not secrets).
5. Add a comment explaining these must come from .env or PythonAnywhere env vars.

The wsgi.py should only set non-secret config defaults. All secrets must come from .env file or environment variables set in the PythonAnywhere dashboard.
  </action>
  <verify>
    <automated>python -c "t1=open('backend/test_phase1.py','r',encoding='utf-8').read(); assert 'do-not-use-in-production' in t1, 'test_phase1 not fixed'; t3=open('backend/test_phase3.py','r',encoding='utf-8').read(); assert 'admin2025' not in t3, 'test_phase3 still has real passwords'; w=open('backend/dashboard/wsgi.py','r',encoding='utf-8').read(); assert 'bangko-admin-secret-key' not in w, 'wsgi still has insecure default'; assert 'finance2025' not in w, 'wsgi still has finance password'; print('Test secrets and wsgi.py OK')"</automated>
  </verify>
  <done>Test files use obviously-fake placeholder secrets; wsgi.py no longer hardcodes any credentials or secret keys in os.environ.</done>
</task>

</tasks>

<verification>
1. `grep -n "Username cannot be empty" backend/dashboard/admin_dashboard.py` -- match found
2. `grep -n "Password cannot be empty" backend/dashboard/admin_dashboard.py` -- match found
3. `grep -n "and admin_user" backend/dashboard/admin_dashboard.py` -- match found in login comparison
4. `grep -n "test-secret-key\"" backend/test_phase1.py` -- should NOT match (old value gone)
5. `grep -n "do-not-use" backend/test_phase1.py` -- should match
6. `grep -n "admin2025" backend/test_phase3.py` -- should NOT match (old passwords gone)
7. `grep -n "bangko-admin-secret-key" backend/dashboard/wsgi.py` -- should NOT match
8. `grep -n "finance2025" backend/dashboard/wsgi.py` -- should NOT match
</verification>

<success_criteria>
- Empty username/password POST to /login returns 400 with specific field error messages
- Admin login comparison includes `and admin_user` to prevent blank env var match
- All test files use obviously-fake placeholder credentials
- wsgi.py contains no hardcoded secrets or insecure defaults
</success_criteria>

<output>
After completion, create `.planning/phases/01-critical-fixes-security/01-03-SUMMARY.md`
</output>
