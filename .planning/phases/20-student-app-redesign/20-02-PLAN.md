---
phase: 20-student-app-redesign
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - mobile/student_app_v2/app/src/main/res/values-night/colors.xml
  - mobile/student_app_v2/app/src/main/res/values-night/themes.xml
  - mobile/student_app_v2/app/src/main/java/com/bankongseton/student/StudentApp.kt
  - mobile/student_app_v2/app/src/main/java/com/bankongseton/student/SettingsActivity.kt
  - mobile/student_app_v2/app/src/main/res/layout/activity_settings.xml
  - mobile/student_app_v2/app/src/main/res/values/strings.xml
  - mobile/student_app_v2/app/src/main/res/layout/activity_home.xml
  - mobile/student_app_v2/app/src/main/res/layout/activity_receipt.xml
  - mobile/student_app_v2/app/src/main/res/layout/item_transaction.xml
  - mobile/student_app_v2/app/src/main/java/com/bankongseton/student/HomeActivity.kt
  - mobile/student_app_v2/app/src/main/java/com/bankongseton/student/ReceiptActivity.kt
  - mobile/student_app_v2/app/src/main/java/com/bankongseton/student/TransactionsAdapter.kt
autonomous: true
requirements:
  - APPA-01
  - APPA-02

must_haves:
  truths:
    - "Dark mode toggle in Settings saves preference and immediately applies theme without restart"
    - "Three options: Follow System / Light / Dark (not old on/off toggle)"
    - "In dark mode, all screens use dark M3 palette — no white screens, no invisible text"
    - "Currency symbol shows ₱ everywhere — NOT ฿"
    - "App preserves theme preference across cold restarts (preference loaded in Application.onCreate)"
  artifacts:
    - path: "mobile/student_app_v2/app/src/main/res/values-night/colors.xml"
      provides: "Dark M3 color roles for night qualifier"
      contains: "md_theme_dark_primary"
    - path: "mobile/student_app_v2/app/src/main/java/com/bankongseton/student/StudentApp.kt"
      provides: "Application class that applies saved theme mode at startup"
      contains: "AppCompatDelegate.setDefaultNightMode"
    - path: "mobile/student_app_v2/app/src/main/java/com/bankongseton/student/SettingsActivity.kt"
      provides: "3-way theme selector UI"
      contains: "MODE_NIGHT_FOLLOW_SYSTEM"
  key_links:
    - from: "StudentApp.kt"
      to: "AppCompatDelegate"
      via: "setDefaultNightMode called in onCreate"
      pattern: "AppCompatDelegate.setDefaultNightMode"
    - from: "SettingsActivity.kt"
      to: "StudentApp.kt"
      via: "SharedPreferences key 'theme_mode'"
      pattern: "theme_mode"
    - from: "values-night/colors.xml"
      to: "values/themes.xml"
      via: "Android resource qualifier system selects night colors automatically"
      pattern: "md_theme_dark_"
---

<objective>
Add dark mode support (values-night colors, AppCompatDelegate 3-way toggle in SettingsActivity) and fix the currency symbol bug (₱ not ฿) throughout the app.

Purpose: Completes the M3 visual design (dark + light), gives users control over appearance, and corrects the existing currency display bug.
Output: values-night/colors.xml, updated SettingsActivity with 3-way toggle, StudentApp.kt applying persisted preference, all ฿ replaced with ₱.
</objective>

<execution_context>
@C:/Users/admin/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-student-app-redesign/20-CONTEXT.md
@.planning/phases/20-student-app-redesign/20-01-SUMMARY.md

<interfaces>
<!-- Currency bug locations (confirmed via grep, 14 occurrences across 5 files): -->
<!-- activity_home.xml: 1 occurrence of ฿ -->
<!-- HomeActivity.kt: 3 occurrences of ฿ -->
<!-- ReceiptActivity.kt: 7 occurrences of ฿ -->
<!-- TransactionsAdapter.kt: 2 occurrences of ฿ (may be in item_transaction.xml too) -->
<!-- Replace ALL with ₱ (U+20B1, Philippine Peso) -->

<!-- SecureStorage class (existing): -->
<!-- mobile/student_app_v2/app/src/main/java/com/bankongseton/student/SecureStorage.kt -->
<!-- Use standard SharedPreferences for theme_mode (not SecureStorage — not sensitive) -->
<!-- SharedPreferences file: "app_prefs", key: "theme_mode", values: "system"|"light"|"dark" -->

<!-- AppCompatDelegate constants: -->
<!-- AppCompatDelegate.MODE_NIGHT_FOLLOW_SYSTEM -->
<!-- AppCompatDelegate.MODE_NIGHT_NO -->
<!-- AppCompatDelegate.MODE_NIGHT_YES -->

<!-- AndroidManifest.xml: -->
<!-- Currently has android:name=".StudentApp" or similar for Application class -->
<!-- If Application class doesn't exist yet, create StudentApp.kt and register in manifest -->

<!-- Dark mode M3 colors (already defined in colors.xml from Plan 01): -->
<!-- md_theme_dark_primary, md_theme_dark_onPrimary, etc. -->
<!-- values-night/colors.xml just re-maps the theme attribute names to the dark variants -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create values-night directory and dark color/theme overrides</name>
  <files>
    mobile/student_app_v2/app/src/main/res/values-night/colors.xml
    mobile/student_app_v2/app/src/main/res/values-night/themes.xml
  </files>
  <action>
**Step A — Create res/values-night/colors.xml:**
This file re-maps the same attribute names used in themes.xml but points them to the dark variants:

```xml
<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Night qualifier: override light colors with dark M3 roles -->
    <color name="md_theme_light_primary">@color/md_theme_dark_primary</color>
    <color name="md_theme_light_onPrimary">@color/md_theme_dark_onPrimary</color>
    <color name="md_theme_light_primaryContainer">@color/md_theme_dark_primaryContainer</color>
    <color name="md_theme_light_onPrimaryContainer">@color/md_theme_dark_onPrimaryContainer</color>
    <color name="md_theme_light_secondary">@color/md_theme_dark_secondary</color>
    <color name="md_theme_light_onSecondary">@color/md_theme_dark_onSecondary</color>
    <color name="md_theme_light_secondaryContainer">@color/md_theme_dark_secondaryContainer</color>
    <color name="md_theme_light_onSecondaryContainer">@color/md_theme_dark_onSecondaryContainer</color>
    <color name="md_theme_light_tertiary">@color/md_theme_dark_tertiary</color>
    <color name="md_theme_light_onTertiary">@color/md_theme_dark_onTertiary</color>
    <color name="md_theme_light_tertiaryContainer">@color/md_theme_dark_tertiaryContainer</color>
    <color name="md_theme_light_onTertiaryContainer">@color/md_theme_dark_onTertiaryContainer</color>
    <color name="md_theme_light_error">@color/md_theme_dark_error</color>
    <color name="md_theme_light_onError">@color/md_theme_dark_onError</color>
    <color name="md_theme_light_errorContainer">@color/md_theme_dark_errorContainer</color>
    <color name="md_theme_light_onErrorContainer">@color/md_theme_dark_onErrorContainer</color>
    <color name="md_theme_light_background">@color/md_theme_dark_background</color>
    <color name="md_theme_light_onBackground">@color/md_theme_dark_onBackground</color>
    <color name="md_theme_light_surface">@color/md_theme_dark_surface</color>
    <color name="md_theme_light_onSurface">@color/md_theme_dark_onSurface</color>
    <color name="md_theme_light_surfaceVariant">@color/md_theme_dark_surfaceVariant</color>
    <color name="md_theme_light_onSurfaceVariant">@color/md_theme_dark_onSurfaceVariant</color>
    <color name="md_theme_light_outline">@color/md_theme_dark_outline</color>
    <color name="md_theme_light_outlineVariant">@color/md_theme_dark_outlineVariant</color>
    <color name="md_theme_light_inverseSurface">@color/md_theme_dark_inverseSurface</color>
    <color name="md_theme_light_inverseOnSurface">@color/md_theme_dark_inverseOnSurface</color>
    <color name="md_theme_light_inversePrimary">@color/md_theme_dark_inversePrimary</color>
    <color name="md_theme_light_surfaceTint">@color/md_theme_dark_surfaceTint</color>
</resources>
```

**Step B — Create res/values-night/themes.xml:**
Override the windowLightStatusBar for dark mode (white icons on dark status bar):

```xml
<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.StudentApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/md_theme_light_background</item>
    </style>
</resources>
```

Note: In values-night, `md_theme_light_background` is already aliased to the dark value (#1A1C1E) via values-night/colors.xml above — so this correctly sets dark background.
  </action>
  <verify>
    <automated>ls mobile/student_app_v2/app/src/main/res/values-night/ && cd mobile/student_app_v2 && ./gradlew assembleDebug 2>&1 | grep -E "error:|BUILD"</automated>
  </verify>
  <done>values-night/ directory exists with colors.xml and themes.xml. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: AppCompatDelegate 3-way toggle in Application + SettingsActivity + fix ₱ currency bug</name>
  <files>
    mobile/student_app_v2/app/src/main/java/com/bankongseton/student/StudentApp.kt
    mobile/student_app_v2/app/src/main/java/com/bankongseton/student/SettingsActivity.kt
    mobile/student_app_v2/app/src/main/res/layout/activity_settings.xml
    mobile/student_app_v2/app/src/main/res/values/strings.xml
    mobile/student_app_v2/app/src/main/res/layout/activity_home.xml
    mobile/student_app_v2/app/src/main/res/layout/activity_receipt.xml
    mobile/student_app_v2/app/src/main/res/layout/item_transaction.xml
    mobile/student_app_v2/app/src/main/java/com/bankongseton/student/HomeActivity.kt
    mobile/student_app_v2/app/src/main/java/com/bankongseton/student/ReceiptActivity.kt
    mobile/student_app_v2/app/src/main/java/com/bankongseton/student/TransactionsAdapter.kt
  </files>
  <action>
**Step A — Create/update StudentApp.kt (Application class):**

Check if StudentApp.kt already exists. If not, create it:

```kotlin
package com.bankongseton.student

import android.app.Application
import androidx.appcompat.app.AppCompatDelegate

class StudentApp : Application() {
    override fun onCreate() {
        super.onCreate()
        applyThemePreference()
    }

    fun applyThemePreference() {
        val prefs = getSharedPreferences("app_prefs", MODE_PRIVATE)
        val mode = prefs.getString("theme_mode", "system") ?: "system"
        val nightMode = when (mode) {
            "light" -> AppCompatDelegate.MODE_NIGHT_NO
            "dark" -> AppCompatDelegate.MODE_NIGHT_YES
            else -> AppCompatDelegate.MODE_NIGHT_FOLLOW_SYSTEM
        }
        AppCompatDelegate.setDefaultNightMode(nightMode)
    }
}
```

Register in AndroidManifest.xml if not already:
```xml
<application
    android:name=".StudentApp"
    ...
```

**Step B — Add 3-way theme selector to SettingsActivity:**

Read the current SettingsActivity.kt. Find the existing dark mode toggle (likely a Switch/CheckBox for boolean dark mode). Replace it with a 3-way RadioGroup approach.

Add to activity_settings.xml (find the existing dark mode section and replace it):
```xml
<!-- Theme selection section -->
<com.google.android.material.card.MaterialCardView
    android:id="@+id/cardTheme"
    style="?attr/materialCardViewOutlinedStyle"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginTop="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/settings_theme"
            android:textAppearance="?attr/textAppearanceTitleMedium" />

        <RadioGroup
            android:id="@+id/rgTheme"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp">

            <com.google.android.material.radiobutton.MaterialRadioButton
                android:id="@+id/rbThemeSystem"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/theme_system" />

            <com.google.android.material.radiobutton.MaterialRadioButton
                android:id="@+id/rbThemeLight"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/theme_light" />

            <com.google.android.material.radiobutton.MaterialRadioButton
                android:id="@+id/rbThemeDark"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/theme_dark" />
        </RadioGroup>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
```

Add to strings.xml:
```xml
<string name="settings_theme">Appearance</string>
<string name="theme_system">Follow system</string>
<string name="theme_light">Light</string>
<string name="theme_dark">Dark</string>
```

In SettingsActivity.kt, add theme preference logic:

```kotlin
// In onCreate(), after existing setup:
val prefs = getSharedPreferences("app_prefs", MODE_PRIVATE)
val rgTheme = findViewById<RadioGroup>(R.id.rgTheme)

// Set initial selection
when (prefs.getString("theme_mode", "system")) {
    "light" -> rgTheme.check(R.id.rbThemeLight)
    "dark" -> rgTheme.check(R.id.rbThemeDark)
    else -> rgTheme.check(R.id.rbThemeSystem)
}

// Listen for changes
rgTheme.setOnCheckedChangeListener { _, checkedId ->
    val mode = when (checkedId) {
        R.id.rbThemeLight -> "light"
        R.id.rbThemeDark -> "dark"
        else -> "system"
    }
    prefs.edit().putString("theme_mode", mode).apply()
    (application as StudentApp).applyThemePreference()
    // Recreate to apply immediately — AppCompatDelegate recreates activity
    recreate()
}
```

Remove any existing boolean dark mode Switch/CheckBox code from SettingsActivity.kt.

**Step C — Fix ₱ currency bug:**

Replace ALL occurrences of the Thai Baht symbol `฿` with Philippine Peso `₱` (U+20B1) in the following files:
- `activity_home.xml` — search and replace ฿ with ₱
- `HomeActivity.kt` — search and replace ฿ with ₱ (3 occurrences in string formatting)
- `ReceiptActivity.kt` — search and replace ฿ with ₱ (7 occurrences)
- `TransactionsAdapter.kt` — search and replace ฿ with ₱ (2 occurrences)
- `item_transaction.xml` — check for ฿ and replace if present

Also add a string resource for the currency symbol in strings.xml:
```xml
<string name="currency_symbol">₱</string>
```

Use `getString(R.string.currency_symbol)` in Kotlin where formatting strings for currency display (preferred approach for maintainability), or do direct string replacement — either is acceptable. The critical requirement is that ₱ appears in all user-visible currency strings and ฿ does not appear anywhere.

Verify the fix: `grep -r "฿" mobile/student_app_v2/app/src/main/` must return no results.
  </action>
  <verify>
    <automated>cd mobile/student_app_v2 && ./gradlew assembleDebug 2>&1 | grep -E "error:|BUILD" && grep -r "฿" app/src/main/ | wc -l</automated>
  </verify>
  <done>
- Build succeeds
- `grep -r "฿" app/src/main/` returns 0 results (all currency symbols are ₱)
- StudentApp.kt exists and is registered as Application class in AndroidManifest.xml
- SettingsActivity has RadioGroup with 3 options (Follow System / Light / Dark)
- SharedPreferences key "theme_mode" persists theme choice
  </done>
</task>

</tasks>

<verification>
1. `./gradlew assembleDebug` — BUILD SUCCESSFUL
2. `grep -r "฿" mobile/student_app_v2/app/src/main/` — zero results
3. `ls mobile/student_app_v2/app/src/main/res/values-night/` — shows colors.xml and themes.xml
4. `grep "AppCompatDelegate" mobile/student_app_v2/app/src/main/java/com/bankongseton/student/StudentApp.kt` — returns match
5. `grep "theme_mode" mobile/student_app_v2/app/src/main/java/com/bankongseton/student/SettingsActivity.kt` — returns match
</verification>

<success_criteria>
- Dark mode works: system/light/dark preference persists across restarts and applies via AppCompatDelegate
- All screens display correct dark palette in dark mode (no white background, no invisible text)
- Currency symbol is ₱ in ALL screens — grep for ฿ returns zero results
- SettingsActivity shows 3-way theme selector (not old boolean toggle)
</success_criteria>

<output>
After completion, create `.planning/phases/20-student-app-redesign/20-02-SUMMARY.md`
</output>
