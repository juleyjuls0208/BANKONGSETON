---
phase: 12-receipt-fcm-wiring
plan: "02"
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - .planning/phases/12-receipt-fcm-wiring/12-01-SUMMARY.md
autonomous: true
requirements:
  - NOTF-01

must_haves:
  truths:
    - "migrate_users_schema() is confirmed called at api_server.py startup in a non-fatal try/except (lines 109-115)"
    - "12-01-SUMMARY.md exists with exact line-number evidence for all three requirements"
    - "Phase 12 is complete: both plans executed, both requirements VERIFIED, config fix applied"
  artifacts:
    - path: "backend/api/api_server.py"
      provides: "migrate_users_schema() startup call (already present — confirm only)"
      contains: "migrate_users_schema"
    - path: ".planning/phases/12-receipt-fcm-wiring/12-01-SUMMARY.md"
      provides: "Phase 12 execution record"
      contains: "VERIFIED"
  key_links:
    - from: "backend/api/api_server.py"
      to: "backend/migrate_transactions.py"
      via: "from migrate_transactions import migrate_users_schema at startup"
      pattern: "migrate_users_schema"
---

<objective>
Confirm that `migrate_users_schema()` is called at `api_server.py` startup (the NOTF-01 dependency). This is the final verification task for Phase 12 — all findings are expected to be already correct; this plan reads the code and confirms.

Purpose: NOTF-01 requires FCM token registration to work correctly. The `migrate_users_schema()` call at startup ensures the Users sheet has the FCMToken column for fresh deployments. The researcher found this present at lines 109-115; this plan confirms it survived any changes since inspection.

Output:
- Confirmed migrate_users_schema() startup call still present in api_server.py
- 12-01-SUMMARY.md written (if Plan 01 executor did not write it)
- Phase 12 marked complete in STATE.md
</objective>

<execution_context>
@C:/Users/admin/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-receipt-fcm-wiring/12-CONTEXT.md
@.planning/phases/12-receipt-fcm-wiring/12-RESEARCH.md
@.planning/phases/12-receipt-fcm-wiring/12-01-SUMMARY.md

<interfaces>
<!-- Expected state of api_server.py at startup — from researcher's direct inspection -->

From backend/api/api_server.py lines 109-115:
```python
# Run schema migration at startup (idempotent — skips if all columns exist)
try:
    from migrate_transactions import migrate_users_schema
    migrate_users_schema()
except Exception as _mig_err:
    logger.warning("event=migrate_users_startup_failed error=%s", _mig_err)
```

This is ALREADY CORRECT. The task is to read and confirm, not to write.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm migrate_users_schema() startup call in api_server.py</name>
  <files>backend/api/api_server.py</files>
  <action>
Read `backend/api/api_server.py` lines 100-125. Confirm:

1. `migrate_users_schema()` is imported from `migrate_transactions` and called unconditionally at module startup (not inside a route handler or conditional block).
2. The call is wrapped in a `try/except Exception` so a Sheets API outage at startup logs a warning but does not crash the server.
3. The call happens after `db = get_sheets_client()` (confirming Sheets client exists before migration runs).

**If the call is present:** Note the exact line numbers and move to Task 2.
**If the call is missing:** Add it in the startup sequence after `db = get_sheets_client()`:
```python
try:
    from migrate_transactions import migrate_users_schema
    migrate_users_schema()
except Exception as _mig_err:
    logger.warning("event=migrate_users_startup_failed error=%s", _mig_err)
```
Then re-confirm the insertion is correct before proceeding.
  </action>
  <verify>
```
grep -n "migrate_users_schema" backend/api/api_server.py
```
Expected output: at least one line showing the import and one line showing the call, both within a try/except block.
  </verify>
  <done>migrate_users_schema() is confirmed called at api_server.py startup in a non-fatal try/except. Exact line numbers recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Write Phase 12 SUMMARY and mark phase complete</name>
  <files>.planning/phases/12-receipt-fcm-wiring/12-01-SUMMARY.md</files>
  <action>
Check if `.planning/phases/12-receipt-fcm-wiring/12-01-SUMMARY.md` already exists (Plan 01 executor should have written it). If it exists, read it and append a "Plan 02 addendum" section with the migrate_users_schema confirmation from Task 1.

If 12-01-SUMMARY.md does NOT exist, create it now with the full Phase 12 execution record:

```markdown
# Phase 12: Receipt & FCM Wiring — Execution Summary

**Completed:** 2026-03-02
**Plans:** 12-01, 12-02
**Method:** Static code inspection (no runtime testing required per CONTEXT.md)

## Requirements Verified

| Requirement | Status | Evidence |
|-------------|--------|----------|
| APP-03 | ✅ VERIFIED | cashier_routes.py:520-532 — 11-column transaction_row, BalanceBefore at col 7, ItemsJson at col 11 |
| NOTF-01 | ✅ VERIFIED | cashier_routes.py:570-612 — send_low_balance_push() called when new_balance < threshold; FCM block non-blocking |
| migrate startup | ✅ VERIFIED | api_server.py:109-115 — migrate_users_schema() called at startup in non-fatal try/except |

## Code Changes Made

| File | Change | Reason |
|------|--------|--------|
| backend/config_validator.py:237 | Added 'ItemsJson' as 11th column in Transactions Log schema | Runtime writes 11 columns; validator previously expected only 10 |

## Documents Updated

| File | Change |
|------|--------|
| .planning/INTEGRATION_AUDIT.md | APP-03 and NOTF-01 marked ✅ VERIFIED in Part 1 requirements table |
| .planning/phases/12-receipt-fcm-wiring/12-VERIFICATION.md | Already written by researcher before plan execution |

## Contradiction Resolution

The Integration Audit (2026-03-01) claimed APP-03 and NOTF-01 were broken. This contradicted Phase 7 VERIFICATION. Direct static inspection confirmed Phase 7 DID fix both issues. The audit predated Phase 7 and was accurate at time of writing. Phase 11 (cashier security hardening) introduced a ~220-line shift in cashier_routes.py, moving route handlers from ~line 307 to ~line 527. All features survived the shift intact.
```

Then update STATE.md to record Phase 12 as complete:
- Add to the completed phases list: "Phase 12: Receipt & FCM Wiring — completed 2026-03-02"
- Add decisions: "APP-03 VERIFIED at cashier_routes.py:520-532; NOTF-01 VERIFIED at cashier_routes.py:570-612; config_validator.py updated to 11-column schema"
  </action>
  <verify>
Read the SUMMARY file and confirm it contains VERIFIED status for all three requirements (APP-03, NOTF-01, migrate startup).
Read STATE.md and confirm Phase 12 completion is recorded.
  </verify>
  <done>12-01-SUMMARY.md exists with full execution record. STATE.md records Phase 12 as complete with key verification line numbers.</done>
</task>

</tasks>

<verification>
Final checks after both tasks complete:

```
# Confirm migrate call exists in api_server.py
grep -n "migrate_users_schema" backend/api/api_server.py

# Confirm config fix from Plan 01 is still in place
grep -n "ItemsJson" backend/config_validator.py

# Confirm SUMMARY exists
ls .planning/phases/12-receipt-fcm-wiring/
```

Expected: migrate_users_schema appears in api_server.py, ItemsJson appears in config_validator.py, SUMMARY file exists.
</verification>

<success_criteria>
- migrate_users_schema() confirmed present at api_server.py startup (exact line number documented)
- 12-01-SUMMARY.md exists with VERIFIED status for APP-03, NOTF-01, and migrate startup
- STATE.md updated to reflect Phase 12 completion
- All three requirements (APP-03, NOTF-01, migrate) have exact line-number evidence on record
</success_criteria>

<output>
Phase 12 is complete. Both plans executed. All deliverables:
- backend/config_validator.py — 11-column Transactions Log schema (ItemsJson added)
- .planning/INTEGRATION_AUDIT.md — APP-03 and NOTF-01 marked ✅ VERIFIED
- .planning/phases/12-receipt-fcm-wiring/12-VERIFICATION.md — full evidence report (written by researcher)
- .planning/phases/12-receipt-fcm-wiring/12-01-SUMMARY.md — execution summary

Next phase to execute: Phase 13 (NFC Payment Contract Fix) or Phase 8 (Security + Reliability Fixes).
</output>
