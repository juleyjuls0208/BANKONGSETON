---
phase: 21
plan: 08
type: execute
wave: 3
depends_on: [21-01, 21-02, 21-03, 21-04, 21-05, 21-06, 21-07]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/PROJECT.md
  - CHANGELOG.md
autonomous: true
requirements: [V11-NFCA-01, V11-PAR-01-06, PROD-HARDEN, V12-EMAIL, V12-STATION, V12-ARDUINO-R3, V12-SMS, V12-CSV]

must_haves:
  truths:
    - "REQUIREMENTS.md contains all Phase 21 requirement IDs with descriptions"
    - "ROADMAP.md Phase 21 entry is fully filled in (not placeholder) and lists all 8 plans"
    - "STATE.md reflects Phase 21 as complete with all plans checked"
    - "PROJECT.md version is bumped to v1.2"
    - "CHANGELOG.md has a v1.2 section documenting all Phase 21 changes"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase 21 requirement definitions"
      contains: "V11-NFCA-01"
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 21 roadmap entry"
      contains: "21-01-PLAN.md"
    - path: ".planning/STATE.md"
      provides: "Current project phase/status"
      contains: "Phase 21"
    - path: ".planning/PROJECT.md"
      provides: "Project metadata"
      contains: "v1.2"
    - path: "CHANGELOG.md"
      provides: "Release notes"
      contains: "v1.2"
  key_links:
    - from: "ROADMAP.md Phase 21 entry"
      to: "8 PLAN.md files in .planning/phases/21-21/"
      via: "- [x] 21-NN-PLAN.md — description lines"
      pattern: "21-0[1-8]-PLAN"
    - from: "REQUIREMENTS.md"
      to: "ROADMAP.md requirements field"
      via: "Requirement IDs referenced in both places"
      pattern: "V1[12]-|PROD-HARDEN"
---

<objective>
Update all planning files to reflect the completion of Phase 21. This is the bookkeeping plan: REQUIREMENTS.md gets Phase 21 IDs, ROADMAP.md gets its real Phase 21 entry, STATE.md is updated, PROJECT.md is bumped to v1.2, and CHANGELOG.md documents the release.

Purpose: Phase 21 execution leaves all planning artefacts in sync so future sessions start with accurate context.
Output: 5 updated planning files.
</objective>

<execution_context>
@C:/Users/admin/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-21/21-CONTEXT.md
@.planning/phases/21-21/21-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 21 requirements to REQUIREMENTS.md and update ROADMAP.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
### REQUIREMENTS.md

Read `.planning/REQUIREMENTS.md`. Append a new section at the end titled `## Phase 21: v1.1 Gap Closure + v1.2 Features` with the following requirement definitions:

```markdown
## Phase 21: v1.1 Gap Closure + v1.2 Features

### Gap Closure (v1.1)

| ID | Description |
|----|-------------|
| V11-NFCA-01 | Fix NFCA-01 regressions: FCM token not sent to backend; HCE token not restored on app restart; stray google-services.json copies causing build failures |
| V11-PAR-01-06 | Fix PAR-01–06 regression: parent login silently swallows Google Sheets connection errors instead of returning HTTP 503 |

### Production Hardening

| ID | Description |
|----|-------------|
| PROD-HARDEN | Remove 6 debug console.log calls from dashboard.html; flip FLASK_DEBUG default to false in admin_dashboard.py and web_app.py |

### New Features (v1.2)

| ID | Description |
|----|-------------|
| V12-EMAIL | Send low-balance email alert to parent when student balance drops below configurable threshold after NFC payment |
| V12-STATION | Multi-canteen station support: STATION_ID env var injected as X-Station-ID header in ArduinoBridge; recorded in Transactions Log |
| V12-ARDUINO-R3 | Arduino R3 auto-connect: STATION_SERIAL_PORT env var triggers automatic serial connection on ArduinoBridge startup |
| V12-SMS | SMS notification via Twilio: send low-balance or payment SMS to parent/student phone number after NFC payment |
| V12-CSV | Bulk CSV student import: POST /api/students/import endpoint in both admin_dashboard.py and web_app.py; handles Excel BOM; per-row error reporting without aborting batch |
```

### ROADMAP.md

Read `.planning/ROADMAP.md`. Replace the placeholder Phase 21 section:

```
### Phase 21: 21

**Goal:** [To be planned]
**Requirements**: TBD
**Depends on:** Phase 20
**Plans:** 0 plans

Plans:
- [ ] TBD (run /gsd:plan-phase 21 to break down)
```

with:

```markdown
### Phase 21: v1.1 Gap Closure + v1.2 Features

**Goal:** Close NFCA-01 and PAR-01–06 regressions from Phases 16/19, harden production config, and ship five v1.2 features: low-balance email, SMS notifications, multi-canteen station support, Arduino R3 auto-connect, and bulk CSV student import.

**Requirements:** V11-NFCA-01, V11-PAR-01-06, PROD-HARDEN, V12-EMAIL, V12-STATION, V12-ARDUINO-R3, V12-SMS, V12-CSV

**Depends on:** Phase 20.1

**Status:** ✓ Complete

**Plans:** 8 plans

Plans:
- [x] 21-01-PLAN.md — v1.1 Bug Fixes: FCM token registration, HCE token restore, parent login 503, delete stray google-services.json [Wave 1]
- [x] 21-02-PLAN.md — Production Hardening: remove 6 debug console.log, flip FLASK_DEBUG to false [Wave 1]
- [x] 21-03-PLAN.md — Low Balance Email + Multi-Station + Arduino R3 auto-connect [Wave 1]
- [x] 21-04-PLAN.md — SMS Notifications via Twilio [Wave 1]
- [x] 21-05-PLAN.md — Bulk CSV Student Import endpoint [Wave 1]
- [x] 21-06-PLAN.md — NFCA-01 Verification artifact [Wave 2]
- [x] 21-07-PLAN.md — PAR-01–06 Verification artifact [Wave 2]
- [x] 21-08-PLAN.md — Planning file updates: REQUIREMENTS, ROADMAP, STATE, PROJECT, CHANGELOG [Wave 3]
```

Also update the overview table in ROADMAP.md to add Phase 21 as a new row:
Find the table row for Phase 20 and add after it:
`| 21 | v1.1 Gap Closure + v1.2 Features | V11-NFCA-01, V11-PAR-01-06, PROD-HARDEN, V12-* | ✓ Complete |`

Update `**Total phases:**` from `5` to `6` (or whatever the count is after Phase 21 is added).
  </action>
  <verify>
`grep -c "V11-NFCA-01" .planning/REQUIREMENTS.md` → at least 1
`grep -c "V12-CSV" .planning/REQUIREMENTS.md` → at least 1
`grep -c "21-01-PLAN.md" .planning/ROADMAP.md` → at least 1
`grep -c "21-08-PLAN.md" .planning/ROADMAP.md` → at least 1
  </verify>
  <done>REQUIREMENTS.md has all 8 Phase 21 requirement IDs defined; ROADMAP.md Phase 21 entry shows all 8 plans as complete</done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md, PROJECT.md version, and CHANGELOG.md</name>
  <files>.planning/STATE.md, .planning/PROJECT.md, CHANGELOG.md</files>
  <action>
### STATE.md

Read `.planning/STATE.md`. Find the current phase status section and update it to reflect Phase 21 as the current (just-completed) phase:
- Set current phase to `21`
- Set phase status to `complete`
- Add Phase 21 to the completed phases list if it isn't already there
- Update any "next phase" pointers accordingly (Phase 22 is not yet planned — note as TBD)

If STATE.md has a "Last updated" field, update it to today's date.

### PROJECT.md

Read `.planning/PROJECT.md`. Find the version field (will look like `version: v1.1` or `**Version:** v1.1` or similar). Update it from `v1.1` to `v1.2`. Also update any "last updated" or "date" field to today's date.

### CHANGELOG.md

Read `CHANGELOG.md` (in the project root). Prepend (insert at the top, after any title line) a new v1.2 section:

```markdown
## [v1.2] — 2026-03-08

### Bug Fixes (v1.1 Gap Closure)
- **FCM token registration**: `FCMService.onNewToken()` now calls `ApiClient.registerFcmToken()` so push notifications are registered with the backend; also called on login
- **HCE token restore**: `BankoHceService.processApdu()` now reloads token from `SecureStorage` when `currentToken` is null after process kill
- **Parent login error handling**: `api_server.py` no longer silently swallows `ConnectionError`/`TimeoutError` from Google Sheets — now returns HTTP 503 with JSON error body
- **Build fix**: Removed stray `google-services.json` copies from `app/` and `app/src/main/java/…/`; canonical copy is `app/src/google-services.json`

### Production Hardening
- Removed 6 `console.log('[DEBUG]…')` calls from `dashboard.html`
- `FLASK_DEBUG` now defaults to `"false"` in `admin_dashboard.py` and `web_app.py`

### New Features
- **Low balance email**: Parent receives email alert when student balance drops below configurable threshold (env: `LOW_BALANCE_THRESHOLD`, default 50) after NFC payment
- **Multi-canteen station support**: `STATION_ID` env var (default `"main"`) sent as `X-Station-ID` header in ArduinoBridge requests and recorded in Transactions Log
- **Arduino R3 auto-connect**: `STATION_SERIAL_PORT` env var triggers automatic serial port connection on ArduinoBridge startup
- **SMS notifications (Twilio)**: `TwilioSMSNotifier` class added to `notifications.py`; triggered after NFC payment if `TWILIO_*` env vars set
- **Bulk CSV student import**: `POST /api/students/import` in `admin_dashboard.py` and `web_app.py`; handles Excel BOM (`utf-8-sig`); per-row validation without aborting batch; returns `{imported, skipped, errors}`
```

If CHANGELOG.md does not exist, create it with just the above section.
  </action>
  <verify>
`grep -c "v1.2" .planning/PROJECT.md` → at least 1
`grep -c "v1.2" CHANGELOG.md` → at least 1
`grep -c "Phase 21" .planning/STATE.md` → at least 1
  </verify>
  <done>PROJECT.md shows v1.2; CHANGELOG.md has a [v1.2] section with all Phase 21 changes; STATE.md reflects Phase 21 as complete</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -c "V11-NFCA-01" .planning/REQUIREMENTS.md` → ≥ 1
2. `grep -c "V12-CSV" .planning/REQUIREMENTS.md` → ≥ 1
3. `grep -c "21-08-PLAN.md" .planning/ROADMAP.md` → ≥ 1
4. `grep -c "v1.2" .planning/PROJECT.md` → ≥ 1
5. `grep -c "v1.2" CHANGELOG.md` → ≥ 1
6. `grep -c "Phase 21" .planning/STATE.md` → ≥ 1
</verification>

<success_criteria>
- All 8 Phase 21 requirement IDs defined in REQUIREMENTS.md
- ROADMAP.md Phase 21 entry fully filled with goal, requirements, 8 plans all marked complete
- STATE.md current phase = 21, status = complete
- PROJECT.md version = v1.2
- CHANGELOG.md [v1.2] section documents all bug fixes and new features from Phase 21
</success_criteria>

<output>
After completion, create `.planning/phases/21-21/21-08-SUMMARY.md` using the summary template at `@C:/Users/admin/.config/opencode/get-shit-done/templates/summary.md`
</output>
