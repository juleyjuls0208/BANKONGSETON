---
phase: 21
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/dashboard/templates/dashboard.html
  - backend/dashboard/admin_dashboard.py
  - backend/dashboard/web_app.py
autonomous: true
requirements: [PROD-HARDEN]

must_haves:
  truths:
    - "No debug console.log statements appear in dashboard.html"
    - "FLASK_DEBUG defaults to false in both admin_dashboard.py and web_app.py"
  artifacts:
    - path: "backend/dashboard/templates/dashboard.html"
      provides: "Production-clean frontend with no debug logging"
      contains: "// console.log removed"
    - path: "backend/dashboard/admin_dashboard.py"
      provides: "FLASK_DEBUG defaulting to false"
      contains: "\"false\""
    - path: "backend/dashboard/web_app.py"
      provides: "FLASK_DEBUG defaulting to false"
      contains: "\"false\""
  key_links:
    - from: "dashboard.html"
      to: "browser console"
      via: "removal of 6 console.log('[DEBUG]...') calls"
      pattern: "console.log"
    - from: "admin_dashboard.py"
      to: "Flask debug mode"
      via: "os.getenv('FLASK_DEBUG', 'false')"
      pattern: "FLASK_DEBUG"
---

<objective>
Remove six debug console.log statements from dashboard.html and flip FLASK_DEBUG defaults from "true" to "false" in both dashboard server files to ensure production-safe behaviour.

Purpose: Prevent debug noise in production browser consoles and eliminate accidental Flask debug mode in deployed environments.
Output: Three clean files with no debug output paths left open.
</objective>

<execution_context>
@C:/Users/admin/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-21/21-CONTEXT.md
@.planning/phases/21-21/21-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove 6 debug console.log lines from dashboard.html</name>
  <files>backend/dashboard/templates/dashboard.html</files>
  <action>
Delete the following six lines (exact line numbers from research; verify by searching for the string before deleting):

- Line 448: `console.log('[DEBUG] ...')` — search for `[DEBUG]` near the balance update function
- Line 452: `console.log('[DEBUG] ...')` — nearby debug call
- Line 786: `console.log('[DEBUG] ...')` — search for `[DEBUG]` near the transaction section
- Line 790: `console.log('[DEBUG] ...')` — nearby debug call
- Line 807: `console.log('[DEBUG] ...')` — nearby debug call
- Line 809: `console.log('[DEBUG] ...')` — nearby debug call

Search strategy: `grep -n "console.log.*\[DEBUG\]" backend/dashboard/templates/dashboard.html` to find the exact lines, then delete each matching line entirely (do not comment out — remove completely).

Do NOT remove any console.error or console.warn lines — only remove lines matching `console.log` that contain `[DEBUG]`.
  </action>
  <verify>
`grep -c "console.log.*\[DEBUG\]" backend/dashboard/templates/dashboard.html` must output `0`
  </verify>
  <done>Zero [DEBUG] console.log lines remain in dashboard.html</done>
</task>

<task type="auto">
  <name>Task 2: Flip FLASK_DEBUG default to false in admin_dashboard.py and web_app.py</name>
  <files>
    backend/dashboard/admin_dashboard.py
    backend/dashboard/web_app.py
  </files>
  <action>
In `admin_dashboard.py` (around line 2944), find:
```python
os.getenv("FLASK_DEBUG", "true")
```
Replace with:
```python
os.getenv("FLASK_DEBUG", "false")
```

In `web_app.py` (around line 2516), find the same pattern and apply the same replacement.

In both files, also ensure the value is used correctly — it is typically compared with `.lower() == "true"` or passed to `app.run(debug=...)`. Do not change how the value is consumed, only change the default string from `"true"` to `"false"`.

Do NOT touch `api_server.py` — `API_DEBUG` there already defaults to `"False"` and requires no change.
  </action>
  <verify>
`grep -n "FLASK_DEBUG.*true" backend/dashboard/admin_dashboard.py` must return no results (no lowercase "true" default)
`grep -n "FLASK_DEBUG.*true" backend/dashboard/web_app.py` must return no results
`python -m py_compile backend/dashboard/admin_dashboard.py && echo PASS`
`python -m py_compile backend/dashboard/web_app.py && echo PASS`
  </verify>
  <done>Both dashboard files default FLASK_DEBUG to "false"; both pass py_compile</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -c "console.log.*\[DEBUG\]" backend/dashboard/templates/dashboard.html` → `0`
2. `grep -n "FLASK_DEBUG" backend/dashboard/admin_dashboard.py` → shows `"false"` default
3. `grep -n "FLASK_DEBUG" backend/dashboard/web_app.py` → shows `"false"` default
4. `python -m py_compile backend/dashboard/admin_dashboard.py && python -m py_compile backend/dashboard/web_app.py && echo ALL PASS`
</verification>

<success_criteria>
- No [DEBUG] console.log lines remain in dashboard.html
- FLASK_DEBUG defaults to "false" in admin_dashboard.py and web_app.py
- All three files pass syntax checks
</success_criteria>

<output>
After completion, create `.planning/phases/21-21/21-02-SUMMARY.md` using the summary template at `@C:/Users/admin/.config/opencode/get-shit-done/templates/summary.md`
</output>
