---
phase: 02-code-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/errors.py
  - _archive/web_app_complete.py
  - _archive/mobile/BankongSetonApp
autonomous: true
requirements:
  - QUAL-01
  - QUAL-04

must_haves:
  truths:
    - "setup_logging() emits key=value format lines to stdout only — no file handler, no logs/ directory created"
    - "get_logger() returns a child of the 'bangko' logger hierarchy"
    - "mobile/BankongSetonApp does not exist at its original path"
    - "backend/dashboard/web_app_complete.py does not exist at its original path"
    - "_archive/ directory exists at repo root with the moved files"
  artifacts:
    - path: "backend/errors.py"
      provides: "setup_logging() console-only + get_logger() returning bangko child logger"
      contains: "StreamHandler"
    - path: "_archive/web_app_complete.py"
      provides: "Archived legacy duplicate"
    - path: "_archive/mobile/BankongSetonApp"
      provides: "Archived mobile app folder"
  key_links:
    - from: "backend/errors.py"
      to: "logging.StreamHandler"
      via: "setup_logging()"
      pattern: "StreamHandler"
    - from: "backend/errors.py"
      to: "logging.getLogger('bangko')"
      via: "setup_logging()"
      pattern: "getLogger\\('bangko'\\)"
---

<objective>
Fix backend/errors.py to emit console-only structured key=value logs, and archive dead code files to _archive/.

Purpose: Establishes the logging infrastructure that Plan 04 (print() replacement) depends on. Removing dead files reduces confusion for future contributors. These two tasks are independent of each other but share this plan since neither touches the same files as Plans 01 or 03.

Output: Updated errors.py (console-only, key=value format); _archive/ directory with web_app_complete.py and mobile/BankongSetonApp moved into it.
</objective>

<execution_context>
@C:/Users/admin/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-code-quality/02-CONTEXT.md
@.planning/phases/02-code-quality/02-RESEARCH.md
@backend/errors.py
</context>

<tasks>

<task type="auto" effort="medium">
  <name>Task 1: Rewrite setup_logging() in errors.py to console-only key=value format</name>
  <files>backend/errors.py</files>
  <action>
Read backend/errors.py in full first. Rewrite setup_logging() as follows:

```python
def setup_logging(log_level: str = 'INFO') -> logging.Logger:
    """Configure console-only structured key=value logging. Call once at startup."""
    logger = logging.getLogger('bangko')
    logger.setLevel(getattr(logging, log_level.upper(), logging.INFO))
    logger.handlers.clear()          # Prevent duplicate handlers on re-call
    logger.propagate = False         # Don't leak to root logger

    handler = logging.StreamHandler()  # stdout only — NO file handler
    handler.setLevel(logging.DEBUG)
    handler.setFormatter(logging.Formatter(
        fmt='level=%(levelname)s logger=%(name)s %(message)s'
    ))
    logger.addHandler(handler)
    return logger
```

Key changes from the current implementation:
- REMOVE any FileHandler, RotatingFileHandler, or file-based handler code
- REMOVE the `log_dir` parameter (or keep as no-op if removing breaks callers — check all callers first)
- REMOVE any os.makedirs('logs/') or similar directory creation
- ADD `logger.propagate = False` to prevent double-printing to root logger
- ADD `logger.handlers.clear()` before adding new handler (prevents duplicate handlers if called twice)
- KEEP get_logger(name) function unchanged: it returns logging.getLogger(f'bangko.{name}') or logging.getLogger(name) — verify current implementation and preserve it

Log format MUST be: `level=%(levelname)s logger=%(name)s %(message)s`

After editing: verify no reference to FileHandler, RotatingFileHandler, log_dir, or os.makedirs remains in setup_logging().

AVOID: Changing the get_logger() function signature — downstream files call get_logger(__name__) and must not be broken.
USE: logging.StreamHandler() with no arguments — defaults to sys.stderr. If stdout is required, use logging.StreamHandler(sys.stdout).
  </action>
  <verify>
    <automated>cd backend && python -c "from errors import setup_logging, get_logger; setup_logging(); logger = get_logger('test'); logger.info('event=test_log key=value'); import os; assert not os.path.exists('logs'), 'logs/ dir should not be created'"</automated>
    <manual>Console output should show: level=INFO logger=bangko.test event=test_log key=value (no file created in logs/)</manual>
  </verify>
  <done>setup_logging() runs without creating logs/ directory; emits key=value format to console; no FileHandler in logger.handlers; get_logger() still works</done>
</task>

<task type="auto" effort="low">
  <name>Task 2: Create _archive/ and move dead code files</name>
  <files>_archive/web_app_complete.py, _archive/mobile/BankongSetonApp</files>
  <action>
Archive dead code by moving (not deleting) identified dead files to _archive/ at repo root.

Steps:
1. Create _archive/ directory at repo root (if not exists)
2. Create _archive/mobile/ subdirectory
3. Move backend/dashboard/web_app_complete.py → _archive/web_app_complete.py
4. Move mobile/BankongSetonApp/ → _archive/mobile/BankongSetonApp/

Run vulture to identify any additional whole-file dead code beyond these two confirmed items:
  python -m vulture backend/ --min-confidence 90 2>/dev/null

Review vulture output: Only act on results that identify ENTIRE FILES or ENTIRE CLASSES as unused — do NOT remove individual functions, methods, or variables flagged by vulture (Flask routes are flagged as unused by vulture but are discovered via decorators). The 8 known unused imports in admin_dashboard.py and wsgi.py are at-discretion cleanups (Claude's discretion per CONTEXT.md) — clean them up if clearly safe (e.g., `import os` at top when os is not used anywhere in the file).

Files explicitly whitelisted (do NOT archive):
- backend/nfc_payments.py — future-phase placeholder, leave in place
- Any file with @app.route or @socketio.on decorators (they are NOT dead)

After moving:
- Confirm backend/dashboard/web_app_complete.py no longer exists at original path
- Confirm mobile/BankongSetonApp no longer exists at original path
- Confirm _archive/web_app_complete.py exists
- Confirm _archive/mobile/BankongSetonApp/ exists

Note: web_app_complete.py has its own normalize_card_uid at line 121 — this is the legacy copy and does NOT need updating since the file is being archived.

AVOID: Using git rm — just move with os.rename / shell mv so files are preserved.
USE: Python's shutil.move or shell `mv` command.
  </action>
  <verify>
    <automated>python -c "import os; assert not os.path.exists('backend/dashboard/web_app_complete.py'), 'original web_app_complete.py still exists'; assert not os.path.exists('mobile/BankongSetonApp'), 'original BankongSetonApp still exists'; assert os.path.exists('_archive/web_app_complete.py'), '_archive copy missing'; assert os.path.exists('_archive/mobile/BankongSetonApp'), '_archive mobile copy missing'; print('Archive OK')"</automated>
    <manual>Confirm _archive/ directory at repo root contains both archived items</manual>
  </verify>
  <done>_archive/ exists at repo root; web_app_complete.py and mobile/BankongSetonApp are in _archive/; originals no longer exist at their old paths</done>
</task>

</tasks>

<verification>
cd backend && python -c "from errors import setup_logging, get_logger; setup_logging(); get_logger('verify').info('event=smoke_test status=ok')"
python -c "import os; print(os.path.exists('_archive/web_app_complete.py'), os.path.exists('_archive/mobile/BankongSetonApp'))"
</verification>

<success_criteria>
- setup_logging() emits `level=INFO logger=bangko.test event=...` format to console; no logs/ directory created
- get_logger() returns a logger in the 'bangko' hierarchy
- _archive/ exists at repo root with both dead code items moved into it
- backend/dashboard/web_app_complete.py and mobile/BankongSetonApp no longer exist at original paths
</success_criteria>

<output>
After completion, create `.planning/phases/02-code-quality/02-02-SUMMARY.md` documenting:
- Updated setup_logging() signature and format string
- Whether log_dir parameter was removed or kept as no-op
- List of files archived to _archive/
- Any additional unused imports cleaned up (discretion items)
</output>
