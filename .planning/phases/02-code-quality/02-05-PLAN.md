---
phase: 02-code-quality
plan: 05
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - backend/dashboard/admin_dashboard.py
  - backend/dashboard/cashier/cashier_routes.py
  - backend/api/api_server.py
autonomous: true
requirements:
  - QUAL-02
  - QUAL-03

must_haves:
  truths:
    - "normalize_card_uid() is called from backend/utils.py everywhere — no duplicate implementations in admin_dashboard.py or api_server.py"
    - "admin_dashboard.py has zero 'global arduino', 'global card_reading_active' etc. declarations"
    - "All route handlers and read_card_thread access card reader state via card_reader_state.get()/set()"
    - "cashier_routes.py imports normalize_card_uid from utils (not from admin_dashboard)"
    - "The four globals (arduino, arduino_bridge, card_reading_active, pending_student_id) are removed from module scope in admin_dashboard.py"
  artifacts:
    - path: "backend/dashboard/admin_dashboard.py"
      provides: "CardReaderState wired in; old globals removed; normalize_card_uid removed"
      contains: "from utils import card_reader_state"
    - path: "backend/dashboard/cashier/cashier_routes.py"
      provides: "normalize_card_uid imported from utils"
      contains: "from utils import normalize_card_uid"
    - path: "backend/api/api_server.py"
      provides: "normalize_card_uid removed (uses utils.normalize_card_uid)"
      contains: "from utils import normalize_card_uid"
  key_links:
    - from: "backend/dashboard/admin_dashboard.py"
      to: "backend/utils.py"
      via: "from utils import card_reader_state, normalize_card_uid"
      pattern: "from utils import card_reader_state"
    - from: "backend/dashboard/cashier/cashier_routes.py"
      to: "backend/utils.py"
      via: "from utils import normalize_card_uid"
      pattern: "from utils import normalize_card_uid"
    - from: "backend/api/api_server.py"
      to: "backend/utils.py"
      via: "from utils import normalize_card_uid"
      pattern: "from utils import normalize_card_uid"
---

<objective>
Wire backend/utils.py into admin_dashboard.py, cashier_routes.py, and api_server.py: replace the duplicate normalize_card_uid implementations and migrate all global state access to CardReaderState.

Purpose: Completes QUAL-02 and QUAL-03. Depends on Plan 01 (backend/utils.py must exist). This is the heaviest migration: admin_dashboard.py has ~1900 lines with 4 globals accessed in 6+ route handlers and read_card_thread — every reference must be updated to card_reader_state.get()/set().

Output: Three files updated; normalize_card_uid deduplicated; global state access made thread-safe.
</objective>

<execution_context>
@C:/Users/admin/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-code-quality/02-CONTEXT.md
@.planning/phases/02-code-quality/02-RESEARCH.md
@.planning/phases/02-code-quality/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" effort="max">
  <name>Task 1: Migrate global state to CardReaderState in admin_dashboard.py</name>
  <files>backend/dashboard/admin_dashboard.py</files>
  <action>
Read admin_dashboard.py in full before editing. This is ~1900 lines — read all of it.

**Step 1: Add import at top of file (near other local imports)**
```python
from utils import card_reader_state, normalize_card_uid
```

**Step 2: Remove the four global variable declarations at lines 102-106:**
```python
# REMOVE these four lines:
arduino = None
arduino_bridge = None
card_reading_active = False
pending_student_id = None
```

**Step 3: Remove the local normalize_card_uid() function definition (around line 196)**
The entire function definition `def normalize_card_uid(uid):` in admin_dashboard.py must be removed — it is now provided by utils.py.

**Step 4: Update ALL references to the four globals throughout the file**

For every code path that reads or writes arduino, arduino_bridge, card_reading_active, pending_student_id:

BEFORE:
```python
global arduino, card_reading_active
card_reading_active = True
if arduino and arduino.is_open:
    arduino.close()
    arduino = None
```

AFTER:
```python
card_reader_state.set('card_reading_active', True)
arduino = card_reader_state.get('arduino')
if arduino and arduino.is_open:
    arduino.close()
    card_reader_state.set('arduino', None)
```

**Key paths that MUST be updated (from RESEARCH.md — QUAL-03 scope covers ALL paths):**
- read_card_thread function: reads card_reading_active, writes pending_student_id, reads/writes arduino
- connect_serial route handler: writes arduino, arduino_bridge, card_reading_active
- disconnect_serial route handler: reads/writes arduino, card_reading_active
- start_register route handler: reads/writes card_reading_active, pending_student_id
- link_money_card route handler: reads card_reading_active, pending_student_id
- Any other handler that references any of the four globals

**For multi-field atomic updates**, use card_reader_state.update():
```python
card_reader_state.update(card_reading_active=True, pending_student_id=student_id)
```

**Remove all `global arduino`, `global card_reading_active`, etc. declarations** from all function bodies — these shadow the state object and must go.

**Do NOT change any business logic** — only change how state is read/written.

AVOID: Leaving ANY bare `arduino = ...` assignment at module scope or inside functions without going through card_reader_state.set().
AVOID: Any `global` keyword for these four variables — they are no longer module globals.
USE: card_reader_state.get('key') for reads; card_reader_state.set('key', value) for single writes; card_reader_state.update(k1=v1, k2=v2) for atomic multi-key writes.
  </action>
  <verify>
    <automated>python -c "
import ast, pathlib
src = pathlib.Path('backend/dashboard/admin_dashboard.py').read_text()
tree = ast.parse(src)
# Check no 'global' declarations for the four vars
globals_found = [
    node for node in ast.walk(tree)
    if isinstance(node, ast.Global)
    and any(n in node.names for n in ['arduino','arduino_bridge','card_reading_active','pending_student_id'])
]
if globals_found:
    print('FAIL: global declarations still present for state vars')
    exit(1)
# Check normalize_card_uid not defined locally
funcs = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]
if 'normalize_card_uid' in funcs:
    print('FAIL: local normalize_card_uid still defined')
    exit(1)
# Check card_reader_state imported
if 'card_reader_state' not in src:
    print('FAIL: card_reader_state not found in file')
    exit(1)
print('OK')
"</automated>
    <manual>No 'global arduino' etc. declarations remain; normalize_card_uid function definition removed; card_reader_state imported and referenced</manual>
  </verify>
  <done>admin_dashboard.py: four globals removed from module scope; all global declarations for those vars removed from all functions; all read/write access goes through card_reader_state.get()/set()/update(); local normalize_card_uid function removed</done>
</task>

<task type="auto" effort="medium">
  <name>Task 2: Update cashier_routes.py and api_server.py to import normalize_card_uid from utils</name>
  <files>backend/dashboard/cashier/cashier_routes.py, backend/api/api_server.py</files>
  <action>
**cashier_routes.py:**

Read the file in full. The file currently imports normalize_card_uid from admin_dashboard via inline sys.path.insert inside route function bodies (lines ~150-152 and ~228-231).

Step 1: Find all occurrences of inline normalize_card_uid imports inside function bodies. They look like:
```python
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', '..'))
from admin_dashboard import normalize_card_uid
```

Step 2: Replace with a top-level import. The existing sys.path.insert in this file already points at backend/ (one level up from dashboard/, two from cashier/). Add at the top of the file (after existing imports/sys.path setup):
```python
from utils import normalize_card_uid
```

Step 3: Remove all the inline normalize_card_uid import statements from inside the route function bodies — they are now redundant.

Step 4: Verify the sys.path is set up at module level so `from utils import normalize_card_uid` resolves. If the sys.path.insert is only inside functions, move it to module level.

IMPORTANT: Do NOT change the business logic of how normalize_card_uid is called — only the import source changes.

**api_server.py:**

Read the file in full. api_server.py has its own normalize_card_uid implementation at line 144 (the version that does NOT handle None).

Step 1: Remove the entire local normalize_card_uid() function definition from api_server.py.

Step 2: Add import at the top of api_server.py (near other local imports):
```python
from utils import normalize_card_uid
```

Step 3: Verify all existing calls to normalize_card_uid() throughout api_server.py still work — they now call the utils version, which handles None (safe upgrade).

AVOID: Accidentally leaving a stale `from admin_dashboard import normalize_card_uid` anywhere in cashier_routes.py.
AVOID: Any import of normalize_card_uid from a file other than utils.
  </action>
  <verify>
    <automated>python -c "
import pathlib, ast
for fpath in ['backend/dashboard/cashier/cashier_routes.py', 'backend/api/api_server.py']:
    src = pathlib.Path(fpath).read_text()
    # Must not define normalize_card_uid locally
    tree = ast.parse(src)
    local_funcs = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]
    if 'normalize_card_uid' in local_funcs:
        print(f'FAIL: {fpath} still defines normalize_card_uid locally')
        exit(1)
    # Must not import from admin_dashboard
    if 'from admin_dashboard import normalize_card_uid' in src:
        print(f'FAIL: {fpath} still imports from admin_dashboard')
        exit(1)
    # Must reference utils
    if 'from utils import normalize_card_uid' not in src and 'utils.normalize_card_uid' not in src:
        print(f'FAIL: {fpath} does not import from utils')
        exit(1)
    print(f'OK: {fpath}')
"</automated>
    <manual>Both files import normalize_card_uid from utils; no local definitions remain; no admin_dashboard imports for this function</manual>
  </verify>
  <done>cashier_routes.py imports normalize_card_uid from utils at module level (not inline inside functions); api_server.py imports from utils and has no local normalize_card_uid definition; no remaining admin_dashboard.py imports of normalize_card_uid in either file</done>
</task>

</tasks>

<verification>
# Confirm no duplicate normalize_card_uid definitions
python -c "
import ast, pathlib
for f in pathlib.Path('backend').rglob('*.py'):
    if '_archive' in str(f): continue
    try:
        tree = ast.parse(f.read_text())
        funcs = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]
        if 'normalize_card_uid' in funcs:
            print(f'DUPLICATE: {f}')
    except: pass
print('Scan complete')
"

# Confirm no remaining global declarations for state vars
python -c "
import ast, pathlib
src = pathlib.Path('backend/dashboard/admin_dashboard.py').read_text()
tree = ast.parse(src)
bad = [n for n in ast.walk(tree) if isinstance(n, ast.Global) and any(v in n.names for v in ['arduino','arduino_bridge','card_reading_active','pending_student_id'])]
print('global declarations found:', len(bad)) if bad else print('OK: no global declarations for state vars')
"
</verification>

<success_criteria>
- normalize_card_uid defined only in backend/utils.py; no duplicate in admin_dashboard.py or api_server.py
- cashier_routes.py imports normalize_card_uid from utils at module level (not from admin_dashboard inside functions)
- admin_dashboard.py: zero 'global arduino/arduino_bridge/card_reading_active/pending_student_id' declarations
- All state access via card_reader_state.get()/set()/update() in admin_dashboard.py
- All existing tests pass after the migration
</success_criteria>

<output>
After completion, create `.planning/phases/02-code-quality/02-05-SUMMARY.md` documenting:
- Complete list of route handlers and functions updated in admin_dashboard.py
- Whether any state access patterns needed special handling (e.g., read-modify-write sequences)
- Import path solution for cashier_routes.py (module-level vs inline)
- Result of running existing test suite after migration
</output>
