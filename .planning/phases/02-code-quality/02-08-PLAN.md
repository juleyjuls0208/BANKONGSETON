---
phase: 02-code-quality
plan: 08
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
gap_closure: true
requirements: [QUAL-01]

must_haves:
  truths:
    - "Running the backend server produces visible structured key=value log lines in the terminal"
    - "Flask and all runtime dependencies are importable in the current Python environment"
    - "Server starts without ModuleNotFoundError"
  artifacts:
    - path: "backend/dashboard/requirements.txt"
      provides: "Already correct — lists Flask>=3.0.0, gspread>=5.12.0, pyserial>=3.5, pyjwt, psutil, openpyxl, Flask-CORS, Flask-SocketIO, gunicorn"
      note: "No code change needed — packages just need to be installed"
  key_links:
    - from: "backend/dashboard/requirements.txt"
      to: "Python environment"
      via: "pip install"
      pattern: "pip install -r backend/dashboard/requirements.txt"
---

<objective>
Install the runtime web-server dependencies (Flask, gspread, etc.) from backend/dashboard/requirements.txt so the backend server can start and produce structured log output.

Purpose: UAT Test 1 failed with "ModuleNotFoundError: No module named 'flask'" — the requirements.txt is complete and correct but the packages were never installed. This is a developer environment setup gap, not a code issue.
Output: All packages from backend/dashboard/requirements.txt installed; server starts and produces structured log lines.
</objective>

<execution_context>
@C:/Users/admin/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/dashboard/requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install runtime dependencies</name>
  <files></files>
  <action>
    Run the following command from the project root to install all runtime web-server packages:

    ```
    pip install -r backend/dashboard/requirements.txt
    ```

    This installs: Flask>=3.0.0, gspread>=5.12.0, pyserial>=3.5, pyjwt, psutil, openpyxl, Flask-CORS, Flask-SocketIO, gunicorn (and their transitive dependencies).

    Note: Per STATE.md decision [02-01], this project has no venv — packages are installed system-wide. That is the established pattern; do not create a venv.

    If pip reports any version conflicts, resolve them by allowing pip to choose compatible versions (pip install --upgrade -r backend/dashboard/requirements.txt), then re-verify.
  </action>
  <verify>
    <automated>python -c "import flask, gspread, serial, jwt, psutil, openpyxl; print('All runtime deps OK')"</automated>
  </verify>
  <done>All listed packages import without error. "All runtime deps OK" is printed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify server starts with structured log output</name>
  <files>none (human verification step — no files modified)</files>
  <action>Human-verify step. See how-to-verify below.</action>
  <what-built>Runtime dependencies installed; now verifying the server starts and produces structured log output</what-built>
  <how-to-verify>
    1. Open a terminal at the project root
    2. Run: `python backend/dashboard/admin_dashboard.py`
    3. Wait 3–5 seconds for startup (the server will bind to a port)
    4. Look for log lines in this format:
       `level=INFO logger=bangko.admin_dashboard event=dashboard_starting ...`
    5. Press Ctrl+C to stop the server
    6. Confirm: structured key=value log lines appeared (not bare print() text, not a ModuleNotFoundError)
  </how-to-verify>
  <verify>Human confirms structured log output (see how-to-verify above)</verify>
  <done>User types "approved" after confirming structured key=value log lines appeared on server startup</done>
  <resume-signal>Type "approved" if structured log lines appeared. Or describe the actual output / error if something went wrong.</resume-signal>
</task>

</tasks>

<verification>
UAT Test 1 command (verbatim from UAT.md):
```
python backend/dashboard/admin_dashboard.py
```
Expected: Log lines appear with `level=INFO logger=bangko.admin_dashboard event=dashboard_starting ...` format — not raw text or bare print() output. Press Ctrl+C after confirming.
</verification>

<success_criteria>
- `python -c "import flask, gspread, serial, jwt, psutil, openpyxl; print('All runtime deps OK')"` exits 0
- Starting admin_dashboard.py produces structured key=value log lines
- No ModuleNotFoundError on startup
</success_criteria>

<output>
After completion, create `.planning/phases/02-code-quality/02-08-SUMMARY.md`
</output>
