---
phase: 02-code-quality
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/utils.py
  - tests/test_utils.py
autonomous: true
gap_closure: true
requirements: [QUAL-02]

must_haves:
  truths:
    - "normalize_card_uid(None) returns None (not empty string)"
    - "normalize_card_uid('') still returns empty string"
    - "All 19 unit tests pass (test count unchanged)"
    - "Return type annotation reflects Optional return"
  artifacts:
    - path: "backend/utils.py"
      provides: "normalize_card_uid implementation returning None for None input"
      contains: "return None"
    - path: "tests/test_utils.py"
      provides: "Updated test asserting normalize_card_uid(None) is None"
      contains: "is None"
  key_links:
    - from: "tests/test_utils.py"
      to: "backend/utils.py"
      via: "normalize_card_uid(None)"
      pattern: "assert normalize_card_uid\\(None\\) is None"
---

<objective>
Fix normalize_card_uid() so that a None input returns None instead of empty string, aligning implementation with QUAL-02 requirement. Update the test to match.

Purpose: UAT Test 3 failed — `normalize_card_uid(None)` outputs `''` instead of `None`. Both implementation and test were internally consistent but contradict the QUAL-02 spec that None should pass through unchanged.
Output: backend/utils.py with corrected guard + type annotation + docstring; tests/test_utils.py with corrected assertion; all 19 tests still passing.
</objective>

<execution_context>
@C:/Users/admin/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/utils.py
@tests/test_utils.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix normalize_card_uid to return None for None input</name>
  <files>backend/utils.py</files>
  <action>
    Make three targeted edits to backend/utils.py in the normalize_card_uid function (lines 30–55):

    1. **Return type annotation** (line 30): Change `-> str` to `-> str | None`

    2. **Docstring rule 1** (line 34): Change "If *uid* is None or falsy-after-strip → return ''" to "If *uid* is None → return None"

    3. **Returns docstring** (line 48): Change "Normalised UID string, or '' when the input is empty/None." to "Normalised UID string, or None when uid is None, or '' when uid is empty/whitespace-only."

    4. **Guard clause** (line 51): Change `return ""` to `return None` — this is the one-line fix that changes observable behaviour.

    Do NOT change any other lines. The empty-string returns on lines 54 remain correct (empty/whitespace input → "").
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0,'backend'); from utils import normalize_card_uid; r = normalize_card_uid(None); assert r is None, f'Expected None got {r!r}'; print('PASS: normalize_card_uid(None) is None')"</automated>
  </verify>
  <done>normalize_card_uid(None) returns None; normalize_card_uid('') still returns ''; normalize_card_uid('  0abc  ') still returns 'ABC'</done>
</task>

<task type="auto">
  <name>Task 2: Update test assertion and run full test suite</name>
  <files>tests/test_utils.py</files>
  <action>
    Edit tests/test_utils.py line 32:
    - Change: `assert normalize_card_uid(None) == ""`
    - To:     `assert normalize_card_uid(None) is None`

    Also update the docstring on test_normalize_none (line 31):
    - Change: `"""None input returns empty string (not a crash)."""`
    - To:     `"""None input returns None (passes through unchanged)."""`

    No other changes. Then run the full test suite to confirm all 19 tests still pass.
  </action>
  <verify>
    <automated>python -m pytest tests/test_utils.py -q</automated>
    <manual>Output must show "19 passed" with no failures or errors</manual>
  </verify>
  <done>All 19 tests pass. test_normalize_none asserts `is None`. No other test behaviour changed.</done>
</task>

</tasks>

<verification>
```
python -c "import sys; sys.path.insert(0,'backend'); from utils import normalize_card_uid; print(normalize_card_uid('  0abc  '), normalize_card_uid(None), normalize_card_uid('ABC'))"
```
Expected output: `ABC None ABC`

UAT Test 3 command (verbatim from UAT.md):
```
python -c "import sys; sys.path.insert(0,'backend'); from utils import normalize_card_uid; print(normalize_card_uid('  0abc  '), normalize_card_uid(None), normalize_card_uid('ABC'))"
```
Expected: `ABC None ABC`
</verification>

<success_criteria>
- normalize_card_uid(None) returns None (not "")
- normalize_card_uid("") returns "" (unchanged)
- normalize_card_uid("  0abc  ") returns "ABC" (unchanged)
- Return type annotation is `str | None`
- python -m pytest tests/test_utils.py -q reports 19 passed, 0 failed
- UAT Test 3 command prints `ABC None ABC`
</success_criteria>

<output>
After completion, create `.planning/phases/02-code-quality/02-07-SUMMARY.md`
</output>
