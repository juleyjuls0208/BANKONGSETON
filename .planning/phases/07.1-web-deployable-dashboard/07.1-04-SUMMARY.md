---
phase: 07.1-web-deployable-dashboard
plan: 04
subsystem: ui
tags: [cashier, manual-mode, rfid, arduino, google-sheets, jinja2]

# Dependency graph
requires:
  - phase: 07.1-web-deployable-dashboard
    provides: lookup-student endpoint, manual_student_id path in complete_sale, get_ports() ImportError guard
provides:
  - Cashier POS auto-detection: enters manual mode when no Arduino/serial ports found
  - Manual student lookup modal with search-by-name-or-ID and live balance display
  - "Web Mode" pill badge in dashboard sidebar when arduino_available=False
  - Correct Google Sheets transaction row: TXN-ID, correct column order, positive amount, Completed status
affects:
  - Any phase touching cashier_index.html or dashboard.html

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Auto-detect hardware on load: loadPorts() → enterManualMode() if ports empty or error"
    - "Conditional SocketIO init: initWebSocket() only called when !window.manualMode"
    - "Manual mode checkout: selectStudentAndPay() → POST /cashier/api/complete-sale with manual_student_id"
    - "Sheets row format: [TXN-ID, Timestamp, StudentID, MoneyCardNumber, TransactionType, Amount(+), BalanceBefore, BalanceAfter, Completed, '']"

key-files:
  created: []
  modified:
    - backend/dashboard/cashier/templates/cashier_index.html
    - backend/dashboard/templates/dashboard.html
    - backend/dashboard/cashier/cashier_routes.py

key-decisions:
  - "Auto-detect serial ports on DOMContentLoaded — no user action required to enter manual mode"
  - "initWebSocket() skipped in manual mode — avoids unnecessary SocketIO errors on web-only hosting"
  - "complete-sale called directly with manual_student_id (no process-sale step) — process-sale requires SocketIO"
  - "Transaction row uses positive Amount (deduction value) matching all other rows in Transactions Log"
  - "Status='Completed' (not 'Success') to match admin_dashboard.py load transaction pattern"
  - "StudentID resolved from Users sheet in RFID path so all rows have StudentID populated"

patterns-established:
  - "Manual mode badge: #manual-mode-badge shown, port selector hidden, checkout enabled immediately"
  - "Sheets row integrity: all sale paths (RFID, Manual) produce identical 10-column format"

requirements-completed:
  - WEB-04

# Metrics
duration: 15min
completed: 2026-03-01
---

# Phase 07.1 Plan 04: Manual Mode Cashier + Web Mode Badge Summary

**Cashier POS auto-switches to manual lookup mode when no serial ports found; Web Mode badge in sidebar; transaction row column order and format corrected for all sale paths**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-03-01T20:00:00Z
- **Completed:** 2026-03-01T20:15:00Z
- **Tasks:** 3 (2 planned + 1 deviation fix)
- **Files modified:** 3

## Accomplishments
- `cashier_index.html`: DOMContentLoaded → `loadPorts()` → `enterManualMode()` auto-triggers when ports array is empty or fetch fails; SocketIO skipped in manual mode
- Manual student lookup modal: live search against `/cashier/api/lookup-student`, displays name/ID/balance, single click to charge
- `dashboard.html`: `{% if not arduino_available %}` renders subtle "Web Mode" pill badge in sidebar brand area
- `cashier_routes.py`: Transaction row corrected — TXN-ID generated, correct 10-column order, positive amount, `'Completed'` status, empty ErrorMessage; StudentID resolved for RFID path

## Task Commits

Each task was committed atomically:

1. **Task 1: Add manual mode to cashier_index.html** - `b29a5b9` (feat)
2. **Task 2: Add Web Mode badge to dashboard.html** - `c5a462e` (feat)
3. **Deviation: fix sys.modules import pattern** - `e9702f5` (fix)
4. **Deviation: fix Sheets row column order and format** - `fdc4e35` (fix)

**Plan metadata:** _(docs commit follows)_

## Files Created/Modified
- `backend/dashboard/cashier/templates/cashier_index.html` — Auto-detect manual fallback mode, lookup modal, branched checkout flow
- `backend/dashboard/templates/dashboard.html` — Web Mode badge conditionally rendered when arduino_available=False
- `backend/dashboard/cashier/cashier_routes.py` — Transaction row format fixed: TXN-ID, correct column order, positive amount, Completed status, empty ErrorMessage; unused json import removed

## Decisions Made
- Auto-detect on load (no user prompt needed) — transparent hardware detection
- SocketIO init gated on `!window.manualMode` — prevents console errors on web hosting where WebSockets are rejected
- `complete-sale` called directly in manual mode (bypasses `process-sale` SocketIO step) — clean separation of modes
- Transaction row uses `total` (positive) not `-total` — matches all other rows in Transactions Log
- `'Completed'` status string matches admin_dashboard.py load transaction pattern

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] sys.modules import helper needed for cashier_routes.py**
- **Found during:** Task 1 (cashier_index.html implementation revealed runtime import issues)
- **Issue:** `_get_parent_app_module()` helper was missing; cashier_routes.py failed to find parent app module in some entrypoints
- **Fix:** Added `_get_parent_app_module()` sys.modules helper with fallback logic for web_app/admin_dashboard/__main__
- **Files modified:** `backend/dashboard/cashier/cashier_routes.py`
- **Verification:** Syntax check passes
- **Committed in:** `e9702f5`

**2. [Rule 1 - Bug] Google Sheets transaction row wrong column order, negative amount, wrong status, items JSON in ErrorMessage**
- **Found during:** Post-checkpoint user feedback — manual sale row appeared in wrong columns in Sheets
- **Issue:** `transaction_row` in `complete_sale()` used wrong order: `[timestamp, card, type, -total, before, after, 'Success', json.dumps(items)]` instead of the 10-column spec `[TXN-ID, timestamp, student_id, card, type, amount, before, after, 'Completed', '']`
- **Fix:**
  - Added TXN-ID generation: `f"TXN-{get_philippines_time().strftime('%Y%m%d%H%M%S')}"`
  - Added StudentID resolution for RFID path via Users sheet lookup
  - Fixed column order to match Transactions Log spec and admin_dashboard.py pattern
  - Changed amount from `-total` to `total` (positive deduction value)
  - Changed status from `'Success'` to `'Completed'`
  - Changed ErrorMessage from `json.dumps(items)` to `''`
  - Removed now-unused `import json`
- **Files modified:** `backend/dashboard/cashier/cashier_routes.py`
- **Verification:** Python `ast.parse()` syntax check passes; column order verified against `admin_dashboard.py` lines 957-968
- **Committed in:** `fdc4e35`

---

**Total deviations:** 2 auto-fixed (2 bugs)
**Impact on plan:** Both fixes essential for correctness. No scope creep.

## Issues Encountered
None beyond the deviations documented above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 07.1 complete — all 4 plans executed
- Web-deployable dashboard ready: hardware-dependent features gracefully degrade when Arduino absent
- Cashier POS works in both RFID hardware mode and manual web mode
- All Sheets transaction rows now use consistent 10-column format across all sale paths

---
*Phase: 07.1-web-deployable-dashboard*
*Completed: 2026-03-01*
