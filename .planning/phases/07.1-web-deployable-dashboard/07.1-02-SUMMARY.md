---
phase: 07.1-web-deployable-dashboard
plan: 02
subsystem: infra
tags: [wsgi, pythonanywhere, dotenv, flask, deployment]

# Dependency graph
requires:
  - phase: 07.1-web-deployable-dashboard
    provides: web_app.py hardware-free dashboard with startup guards
provides:
  - PythonAnywhere WSGI entry point with correct project path, dotenv loading, absolute credentials path, and deployment checklist
affects: [pythonanywhere-deployment, web-dashboard-hosting]

# Tech tracking
tech-stack:
  added: [python-dotenv (load_dotenv in wsgi.py)]
  patterns: [dotenv-before-import, os.environ.setdefault for non-secret defaults, absolute path for credentials]

key-files:
  created: []
  modified: [backend/dashboard/wsgi.py]

key-decisions:
  - "load_dotenv() called before from web_app import so startup guards in web_app.py fire after secrets are loaded"
  - "os.environ.setdefault() used (not direct assignment) so .env values take precedence over wsgi.py defaults"
  - "GOOGLE_CREDENTIALS_FILE set via os.path.join(project_home, 'config', 'credentials.json') — absolute path avoids CWD-relative resolution issues on PythonAnywhere"
  - "Deployment checklist added as inline comment block listing all required and optional env vars"

patterns-established:
  - "wsgi.py pattern: path → load_dotenv → setdefault non-secrets → import app"

requirements-completed: [WEB-02]

# Metrics
duration: 1min
completed: 2026-03-01
---

# Phase 07.1 Plan 02: wsgi.py Deployment Fix Summary

**wsgi.py rewritten with correct BANKONGSETON path, pre-import dotenv loading, absolute credentials path via os.path.join, and inline deployment checklist covering all required env vars**

## Performance

- **Duration:** 1 min
- **Started:** 2026-03-01T11:46:08Z
- **Completed:** 2026-03-01T11:46:47Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Fixed `project_home` from `/home/bankoseton/FinanceDashboard` → `/home/bankoseton/BANKONGSETON/backend/dashboard`
- Added `load_dotenv()` call BEFORE `from web_app import app` so startup guards in `web_app.py` fire after secrets are loaded
- Changed `GOOGLE_CREDENTIALS_FILE` from bare `'credentials.json'` to `os.path.join(project_home, 'config', 'credentials.json')` for absolute path safety
- Switched from `os.environ[...] =` to `os.environ.setdefault(...)` so `.env` values take precedence
- Added comprehensive deployment checklist comment covering all 4 required env vars plus optional SMTP and admin credentials vars

## Task Commits

Each task was committed atomically:

1. **Task 1: Rewrite wsgi.py with correct path, dotenv loading, absolute credentials path, and deployment checklist** - `fcecefd` (feat)

**Plan metadata:** `c82c821` (docs: complete plan)

## Self-Check: PASSED

## Files Created/Modified
- `backend/dashboard/wsgi.py` - PythonAnywhere WSGI entry point: corrected project path, dotenv loading before app import, absolute credentials path, deployment checklist

## Decisions Made
- `load_dotenv()` placed before `from web_app import app` — web_app.py has module-level startup guards that run at import time; dotenv must populate env vars before those guards execute
- `os.environ.setdefault()` used instead of direct assignment — allows `.env` file values to override wsgi.py defaults without needing to comment out lines
- `GOOGLE_CREDENTIALS_FILE` uses `os.path.join(project_home, 'config', 'credentials.json')` — PythonAnywhere CWD is unpredictable; bare relative path `'credentials.json'` would resolve to wrong directory

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required. (The deployment checklist embedded in wsgi.py guides the PythonAnywhere operator.)

## Next Phase Readiness
- `wsgi.py` is ready for PythonAnywhere deployment
- Ready for Plan 03: remaining deployment tasks in Phase 07.1

---
*Phase: 07.1-web-deployable-dashboard*
*Completed: 2026-03-01*
