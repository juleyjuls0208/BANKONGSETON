---
phase: 07.1-web-deployable-dashboard
plan: "03"
subsystem: api
tags: [flask, gspread, cashier, web-mode, manual-sale, pyserial]

# Dependency graph
requires:
  - phase: 07.1-web-deployable-dashboard
    provides: web_app.py with get_sheets_client/get_philippines_time importable

provides:
  - cashier_routes.py blueprint that loads under web_app.py without circular import
  - GET /cashier/api/lookup-student for manual (web-mode) student search
  - POST /cashier/api/complete-sale with manual_student_id path (no card tap needed)
  - get_ports() returns empty list when pyserial unavailable (triggers frontend manual mode)

affects:
  - 07.1-web-deployable-dashboard Plan 04 (frontend manual-mode cashier UI)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "try: from web_app import ... except ImportError: from admin_dashboard import ... — for blueprint portability"
    - "ImportError catch in get_ports() returning empty list to signal missing hardware to frontend"

key-files:
  created: []
  modified:
    - backend/dashboard/cashier/cashier_routes.py

key-decisions:
  - "try/except import pattern used (not top-level conditional) — local imports inside each function body preserve existing lazy-load pattern"
  - "get_ports() returns {'ports': []} on ImportError — matches Plan 04 frontend enterManualMode() trigger condition"
  - "manual_student_id path reuses get_sheets_client() already imported in complete_sale — no extra connection"
  - "transaction_type='Manual' (not 'Manual Purchase') — consistent with existing short TransactionType values (Purchase, NFC Purchase)"

patterns-established:
  - "Blueprint portability pattern: try web_app import, fallback to admin_dashboard — use for all new cashier routes"

requirements-completed:
  - WEB-03

# Metrics
duration: 2min
completed: 2026-03-01
---

# Phase 07.1 Plan 03: Manual Cashier Mode - Backend Summary

**cashier_routes.py patched for web deployment: try/except import fix, get_ports() pyserial guard, GET /cashier/api/lookup-student, and manual_student_id path in complete_sale with 'Manual' transaction tag**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-01T11:49:04Z
- **Completed:** 2026-03-01T11:51:19Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- Fixed broken `from admin_dashboard import` pattern that prevented cashier blueprint from loading under `web_app.py`
- `get_ports()` now returns `{'ports': []}` when pyserial is absent (was 503), which triggers `enterManualMode()` in the frontend
- Added `GET /cashier/api/lookup-student?q=<term>` endpoint — searches StudentID and Name, returns up to 10 matches with id/name/balance/card_uid
- Extended `POST /cashier/api/complete-sale` to accept `manual_student_id`, resolve card from Users sheet, and tag transaction as `'Manual'`

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix import pattern and pyserial ImportError guard** - `a329e20` (fix)
2. **Task 2: Add lookup-student endpoint and manual_student_id path** - `9c3f66c` (feat)

**Plan metadata:** *(see docs commit below)*

## Files Created/Modified
- `backend/dashboard/cashier/cashier_routes.py` - Fixed import pattern, pyserial guard, lookup-student route, manual sale path

## Decisions Made
- Used `try/except` import inside each function body (not top-level) — preserves existing lazy-load pattern; local imports keep the existing `sys.path.insert` approach working
- `get_ports()` returns `{'ports': []}` on `ImportError` — this is the exact signal Plan 04 frontend uses to enter manual mode
- `transaction_type = 'Manual' if manual_student_id else 'Purchase'` — short type value consistent with existing codebase (not 'Manual Purchase')

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Backend manual-mode support is complete
- Ready for Plan 04: frontend cashier UI changes (manual mode toggle, student search widget, lookup-student API call)
- `get_ports()` returning `[]` will automatically trigger `enterManualMode()` on PythonAnywhere (pyserial absent)

---
*Phase: 07.1-web-deployable-dashboard*
*Completed: 2026-03-01*
