---
phase: 16-nfc-android-hce
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/student_app_v2/app/build.gradle.kts
  - mobile/student_app_v2/app/src/main/AndroidManifest.xml
  - mobile/student_app_v2/app/src/main/java/com/bankongseton/student/Models.kt
  - mobile/student_app_v2/app/src/main/java/com/bankongseton/student/ApiClient.kt
  - mobile/student_app_v2/app/src/main/java/com/bankongseton/student/BankoHceService.kt
  - mobile/student_app_v2/app/src/main/java/com/bankongseton/student/NfcManager.kt
autonomous: true
requirements:
  - NFCA-01

must_haves:
  truths:
    - "BankoHceService responds to SELECT APDU with the stored virtual_card_token"
    - "NfcManager can register a device (calls POST /api/nfc/register with auth token) and store the returned virtual_card_token in EncryptedSharedPreferences"
    - "NfcManager can unregister a device (calls DELETE /api/nfc/unregister) and clears stored token"
    - "Biometric prompt appears on registerDevice; PIN dialog is the fallback when biometric unavailable"
    - "App builds successfully with no compile errors after these changes"
  artifacts:
    - path: mobile/student_app_v2/app/src/main/java/com/bankongseton/student/BankoHceService.kt
      provides: "Android HCE service — handles SELECT APDU, returns token, resets auth flag on deactivation"
      exports: [BankoHceService]
    - path: mobile/student_app_v2/app/src/main/java/com/bankongseton/student/NfcManager.kt
      provides: "Token storage, biometric/PIN auth, register/unregister device API calls"
      exports: [NfcManager]
    - path: mobile/student_app_v2/app/src/main/java/com/bankongseton/student/Models.kt
      provides: "NFC data models"
      contains: "NfcDeviceRequest, NfcRegistrationResponse, NfcUnregisterRequest"
    - path: mobile/student_app_v2/app/src/main/java/com/bankongseton/student/ApiClient.kt
      provides: "BangkoApiService interface with NFC endpoints"
      contains: "registerNfcDevice, unregisterNfcDevice"
  key_links:
    - from: mobile/student_app_v2/app/src/main/java/com/bankongseton/student/BankoHceService.kt
      to: NfcManager.currentToken
      via: "companion object volatile var read in processCommandApdu"
      pattern: "NfcManager\\.currentToken"
    - from: mobile/student_app_v2/app/src/main/java/com/bankongseton/student/NfcManager.kt
      to: ApiClient.apiService
      via: "singleton call — ApiClient.apiService.registerNfcDevice(...)"
      pattern: "ApiClient\\.apiService\\.(registerNfcDevice|unregisterNfcDevice)"
---

<objective>
Port and adapt the HCE infrastructure from mobile/android into student_app_v2. This plan creates the foundational Android HCE files — BankoHceService.kt, NfcManager.kt — and wires them into the existing project (Models, ApiClient, AndroidManifest, build.gradle).

Purpose: Without this plan, NFC payment cannot function at all. Plans 03 and 04 depend on NfcManager being available.
Output: Two new Kotlin files, updated Models/ApiClient/Manifest/build.gradle. App compiles. HCE service registered.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-nfc-android-hce/16-CONTEXT.md

<!-- Source files to PORT from (read-only reference — do NOT modify) -->
@mobile/android/app/src/main/java/com/juls/bankongsetonandroid/BankoHceService.kt
@mobile/android/app/src/main/java/com/juls/bankongsetonandroid/NfcManager.kt
@mobile/android/app/src/main/AndroidManifest.xml

<!-- Target files to UPDATE -->
@mobile/student_app_v2/app/src/main/java/com/bankongseton/student/Models.kt
@mobile/student_app_v2/app/src/main/java/com/bankongseton/student/ApiClient.kt
@mobile/student_app_v2/app/src/main/AndroidManifest.xml
@mobile/student_app_v2/app/build.gradle.kts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NFC models + API endpoints</name>
  <files>
    mobile/student_app_v2/app/src/main/java/com/bankongseton/student/Models.kt
    mobile/student_app_v2/app/src/main/java/com/bankongseton/student/ApiClient.kt
    mobile/student_app_v2/app/build.gradle.kts
  </files>
  <action>
    **Models.kt** — append these three data classes (do not remove existing models):
    ```kotlin
    data class NfcDeviceRequest(val device_id: String, val pin: String)
    data class NfcRegistrationResponse(val virtual_card_token: String)
    data class NfcUnregisterRequest(val device_id: String)
    ```

    **ApiClient.kt** — add two endpoints to the `BangkoApiService` interface:
    ```kotlin
    @POST("nfc/register")
    suspend fun registerNfcDevice(
        @Header("Authorization") authToken: String,
        @Body request: NfcDeviceRequest
    ): Response<NfcRegistrationResponse>

    @DELETE("nfc/unregister")
    suspend fun unregisterNfcDevice(
        @Header("Authorization") authToken: String,
        @Body request: NfcUnregisterRequest
    ): Response<Unit>
    ```
    Keep all existing endpoints untouched.

    **build.gradle.kts** — add biometric dependency in the `dependencies` block (after existing androidx entries):
    ```kotlin
    implementation("androidx.biometric:biometric:1.1.0")
    ```
    The security-crypto dependency for EncryptedSharedPreferences is already present — do NOT add it again.
  </action>
  <verify>
    Run: `cd mobile/student_app_v2 && ./gradlew compileDebugKotlin 2>&1 | grep -E "error:|warning:" | head -20`
    Expected: No errors related to Models.kt or ApiClient.kt
  </verify>
  <done>Models.kt has NfcDeviceRequest, NfcRegistrationResponse, NfcUnregisterRequest; BangkoApiService has registerNfcDevice and unregisterNfcDevice; build.gradle.kts lists biometric:1.1.0</done>
</task>

<task type="auto">
  <name>Task 2: Port BankoHceService + NfcManager</name>
  <files>
    mobile/student_app_v2/app/src/main/java/com/bankongseton/student/BankoHceService.kt
    mobile/student_app_v2/app/src/main/java/com/bankongseton/student/NfcManager.kt
    mobile/student_app_v2/app/src/main/AndroidManifest.xml
  </files>
  <action>
    **BankoHceService.kt** — copy from mobile/android/BankoHceService.kt verbatim, then:
    - Change `package com.juls.bankongsetonandroid` → `package com.bankongseton.student`
    - No other changes needed — the companion object `currentToken`/`isPaymentAuthorized` pattern is identical

    **NfcManager.kt** — copy from mobile/android/NfcManager.kt then apply these targeted changes:
    - Change `package com.juls.bankongsetonandroid` → `package com.bankongseton.student`
    - Replace any `ApiClient.getInstance(context)` call with `ApiClient.apiService` (student_app_v2 uses a singleton object, not an instance-per-context factory)
    - `registerDevice(pin: String, authToken: String, callback: (Boolean) -> Unit)` — add `authToken` as a parameter and pass it as `"Bearer $authToken"` to the `@Header("Authorization")` parameter in `ApiClient.apiService.registerNfcDevice(...)`
    - `unregisterDevice(authToken: String, callback: (Boolean) -> Unit)` — same pattern: add `authToken` param, pass `"Bearer $authToken"` to `unregisterNfcDevice`
    - Keep biometric prompt title "Bangko ng Seton Payment" and subtitle "Authenticate to enable NFC payment" (already in source)
    - Keep PIN fallback dialog (`showPinDialog`) exactly as-is
    - Keep EncryptedSharedPreferences instance — it's NFC-specific data, kept separate from SecureStorage

    **AndroidManifest.xml** — add inside `<manifest>` (before `<application>`):
    ```xml
    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-feature android:name="android.hardware.nfc.hce" android:required="false" />
    ```
    Add inside `<application>` (after existing activities):
    ```xml
    <service
        android:name=".BankoHceService"
        android:exported="true"
        android:permission="android.permission.BIND_NFC_SERVICE">
        <intent-filter>
            <action android:name="android.nfc.cardemulation.action.HOST_APDU_SERVICE" />
            <category android:name="android.intent.category.DEFAULT" />
        </intent-filter>
        <meta-data
            android:name="android.nfc.cardemulation.host_apdu_service"
            android:resource="@xml/apdu_service" />
    </service>
    ```
    Also create `mobile/student_app_v2/app/src/main/res/xml/apdu_service.xml`:
    ```xml
    <host-apdu-service xmlns:android="http://schemas.android.com/apk/res/android"
        android:description="@string/app_name"
        android:requireDeviceUnlock="false">
        <aid-group android:category="payment" android:description="@string/app_name">
            <aid-filter android:name="F0394148148100" />
        </aid-group>
    </host-apdu-service>
    ```
    (Use the same AID as in the legacy app to match the cashier reader configuration.)
  </action>
  <verify>
    Run: `cd mobile/student_app_v2 && ./gradlew compileDebugKotlin 2>&1 | grep -E "^.*error:" | head -20`
    Expected: Zero compile errors
  </verify>
  <done>BankoHceService.kt and NfcManager.kt exist in com.bankongseton.student package; AndroidManifest.xml declares NFC permissions, uses-feature, and BankoHceService; apdu_service.xml exists; app compiles cleanly</done>
</task>

</tasks>

<verification>
After both tasks:
1. `cd mobile/student_app_v2 && ./gradlew assembleDebug` — build must succeed (exit code 0)
2. Confirm `grep -r "BankoHceService" mobile/student_app_v2/app/src/main/AndroidManifest.xml` returns the service declaration
3. Confirm `grep "registerNfcDevice\|unregisterNfcDevice" mobile/student_app_v2/app/src/main/java/com/bankongseton/student/ApiClient.kt` returns 2 lines
</verification>

<success_criteria>
- `./gradlew assembleDebug` exits 0 with no errors
- BankoHceService.kt and NfcManager.kt exist under com.bankongseton.student
- AndroidManifest declares NFC permission + HCE service
- Models.kt has all three NFC data classes
- ApiClient.kt BangkoApiService interface has the two NFC endpoints
</success_criteria>

<output>
After completion, create `.planning/phases/16-nfc-android-hce/16-01-SUMMARY.md`
</output>
