---
phase: 16-nfc-android-hce
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/student_app_v2/app/src/main/java/com/bankongseton/student/ReceiptActivity.kt
autonomous: true
requirements:
  - NFCA-03
  - NFCA-05

must_haves:
  truths:
    - "Opening a receipt for an NFC Purchase transaction does not crash and shows content"
    - "When transaction.items is null or empty, ReceiptActivity shows a single synthetic row: 'NFC Payment' with the transaction total"
    - "ReceiptActivity shows the transaction type label so students can distinguish NFC Purchase from regular Purchase"
    - "Existing Purchase receipts with line items are unaffected"
  artifacts:
    - path: mobile/student_app_v2/app/src/main/java/com/bankongseton/student/ReceiptActivity.kt
      provides: "Receipt display with null-items fallback and transaction type label"
      contains: "NFC Payment synthetic row, transaction type display"
  key_links:
    - from: mobile/student_app_v2/app/src/main/java/com/bankongseton/student/TransactionsAdapter.kt
      to: ReceiptActivity
      via: "Intent extras — transaction object passed on NFC Purchase click"
      pattern: "ReceiptActivity"
---

<objective>
Fix ReceiptActivity so NFC Purchase transactions display correctly. The root cause: `/api/nfc/pay` returns null items, so ReceiptActivity shows nothing. Add a null-items fallback (synthetic "NFC Payment" row) and show the transaction type label.

Purpose: NFCA-03 and NFCA-05 — students must be able to see their NFC payment receipts.
Output: Modified ReceiptActivity.kt. Fully independent of Plan 01 — can run in parallel.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-nfc-android-hce/16-CONTEXT.md

@mobile/student_app_v2/app/src/main/java/com/bankongseton/student/ReceiptActivity.kt
@mobile/student_app_v2/app/src/main/java/com/bankongseton/student/TransactionsAdapter.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add null-items fallback and type label to ReceiptActivity</name>
  <files>
    mobile/student_app_v2/app/src/main/java/com/bankongseton/student/ReceiptActivity.kt
  </files>
  <action>
    Read ReceiptActivity.kt first to understand the existing structure. Then make two targeted changes:

    **Change 1 — Transaction type label:**
    Find where the transaction date/time or merchant name is displayed. Add a TextView (or set an existing one) that shows `transaction.type` (e.g. "NFC Purchase" or "Purchase"). If the layout already has a transaction-type field, populate it. If not, add the text programmatically below the existing header info. Keep it simple — plain text, match existing text style.

    **Change 2 — Null-items fallback:**
    Find the block that iterates `transaction.items` (likely `transaction.items?.forEach { ... }` or similar). Replace it with:
    ```kotlin
    val items = transaction.items
    if (items.isNullOrEmpty()) {
        // Synthetic row for NFC Payment (or any transaction with no line items)
        val syntheticItem = TransactionItem(
            name = "NFC Payment",
            quantity = 1,
            price = transaction.amount  // use whatever field holds total amount
        )
        // render syntheticItem using the same view inflation/binding as a regular item
        // (copy the existing item rendering code and pass syntheticItem)
    } else {
        items.forEach { item ->
            // existing rendering code unchanged
        }
    }
    ```
    Use the actual field names from the existing Transaction/TransactionItem models in Models.kt — read them first. The key constraint: the fallback row must use the same layout/adapter as real items so the UI looks consistent.

    Do NOT change how regular (non-null items) receipts are rendered. Do NOT add new layout files — work within the existing activity_receipt.xml.
  </action>
  <verify>
    1. `cd mobile/student_app_v2 && ./gradlew compileDebugKotlin 2>&1 | grep "error:" | head -10` — must be zero errors
    2. Code review: confirm `isNullOrEmpty()` check exists in ReceiptActivity.kt
    3. Code review: confirm transaction type is displayed somewhere in onCreate/binding setup
  </verify>
  <done>ReceiptActivity compiles; null-items check is present; synthetic "NFC Payment" row path exists; transaction type label is shown</done>
</task>

</tasks>

<verification>
- `./gradlew compileDebugKotlin` exits 0
- `grep -n "isNullOrEmpty\|NFC Payment" mobile/student_app_v2/app/src/main/java/com/bankongseton/student/ReceiptActivity.kt` returns at least 2 lines
- `grep -n "transaction.type\|transactionType\|type" mobile/student_app_v2/app/src/main/java/com/bankongseton/student/ReceiptActivity.kt` returns evidence of type display
</verification>

<success_criteria>
- ReceiptActivity.kt has null/empty items check
- Null items produces a synthetic "NFC Payment" row using transaction total amount
- Transaction type label ("NFC Purchase" / "Purchase") is displayed in the receipt
- Existing Purchase receipts with real items are unaffected
- App still compiles
</success_criteria>

<output>
After completion, create `.planning/phases/16-nfc-android-hce/16-02-SUMMARY.md`
</output>
