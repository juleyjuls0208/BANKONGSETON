---
phase: 27-critical-mobile-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/ios/BankongSetonStudent/Models/LoginModels.swift
  - mobile/ios/BankongSetonStudent/Core/Network/APIClient.swift
  - mobile/ios/BankongSetonStudent/Core/Auth/AuthManager.swift
autonomous: true
requirements:
  - REQ-BUG-MOB-01
  - REQ-BUG-MOB-03

must_haves:
  truths:
    - "After a card is reported lost and then replaced (status returns to active), the student can successfully log in and their card is no longer treated as lost"
    - "Invalid URLs in APIClient never crash the app with a fatal error — they throw a recoverable error instead"
    - "The isCardLost flag in Keychain is NOT deleted during logout (clearAll), so it persists correctly across sessions"
  artifacts:
    - path: "mobile/ios/BankongSetonStudent/Models/LoginModels.swift"
      provides: "Student model with optional cardStatus field"
      contains: "cardStatus: String?"
    - path: "mobile/ios/BankongSetonStudent/Core/Network/APIClient.swift"
      provides: "APIError enum with invalidURL case + no fatalError calls"
      contains: "case invalidURL"
    - path: "mobile/ios/BankongSetonStudent/Core/Auth/AuthManager.swift"
      provides: "clearAll() without isCardLost deletion + login() with active-status clearance"
  key_links:
    - from: "AuthManager.login()"
      to: "student.cardStatus"
      via: "login response model"
      pattern: "cardStatus.*active"
    - from: "AuthManager.clearAll()"
      to: "keysToDelete array"
      via: "Keychain removal"
      pattern: "isCardLost.*keysToDelete"
---

<objective>
Fix two iOS critical bugs: (1) the `isCardLost` Keychain flag being incorrectly wiped on logout so a re-activated card still shows as lost; and (2) two `fatalError()` calls in APIClient that crash the app on invalid URLs instead of throwing a recoverable error.

Purpose: These are production crashes and incorrect UX for students whose cards have been replaced.
Output: Three modified Swift files — LoginModels.swift, APIClient.swift, AuthManager.swift — with both bugs fully resolved.
</objective>

<execution_context>
@C:/Users/admin/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-critical-mobile-fixes/27-RESEARCH.md
@.planning/phases/27-critical-mobile-fixes/27-CONTEXT.md

<interfaces>
<!-- Key facts extracted from RESEARCH.md — executor does NOT need to explore codebase -->

From mobile/ios/BankongSetonStudent/Core/Auth/AuthManager.swift:
- Line ~48: `clearAll()` has a `keysToDelete` array that includes `"isCardLost"` — THIS IS THE BUG
- `login()` function receives a response containing `student: Student`
- `student` is of type `Student` (defined in LoginModels.swift)

From mobile/ios/BankongSetonStudent/Core/Network/APIClient.swift:
- Two `fatalError("Invalid URL")` calls exist — THESE ARE THE BUGS
- `APIError` enum currently does NOT have an `invalidURL` case

From mobile/ios/BankongSetonStudent/Models/LoginModels.swift:
- `Student` struct exists with fields like `name`, `studentId`, etc.
- Does NOT currently have a `cardStatus` field
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cardStatus to Student model + add invalidURL to APIError + replace fatalErrors</name>
  <files>
    mobile/ios/BankongSetonStudent/Models/LoginModels.swift
    mobile/ios/BankongSetonStudent/Core/Network/APIClient.swift
  </files>
  <action>
**LoginModels.swift:**
Add `cardStatus: String?` as an optional field to the `Student` struct. This must be optional because the API may not always return it, and older cached responses won't have it. Keep all existing fields unchanged.

**APIClient.swift:**
1. Find the `APIError` enum and add a new case: `case invalidURL`
2. Search for ALL occurrences of `fatalError("Invalid URL")` (there are exactly 2). Replace each one with `throw APIError.invalidURL`.
3. If the surrounding function is not already `throws`, you must add `throws` to its signature. Check whether the call sites need to be updated to use `try` — if the functions are already async throws, no change needed at call sites.

Do NOT change any other APIError cases or any other logic. Minimal, surgical edits only.
  </action>
  <verify>
    <automated>cd mobile/ios && xcodebuild -scheme BankongSetonStudent -destination "generic/platform=iOS Simulator" build 2>&1 | grep -E "error:|BUILD SUCCEEDED|BUILD FAILED" | tail -20</automated>
  </verify>
  <done>
    - `Student` struct has `cardStatus: String?` field
    - `APIError` enum has `case invalidURL`
    - Zero `fatalError` calls remain in APIClient.swift
    - Build succeeds with no new errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix AuthManager — remove isCardLost from clearAll + clear flag on active login</name>
  <files>
    mobile/ios/BankongSetonStudent/Core/Auth/AuthManager.swift
  </files>
  <action>
**Fix 1 — Remove isCardLost from clearAll():**
In `clearAll()`, find the `keysToDelete` array (around line 48). Remove `"isCardLost"` from this array. Do NOT remove any other keys. The `isCardLost` flag must survive logout so it persists correctly across sessions.

**Fix 2 — Clear isCardLost on successful login when card is active:**
In the `login()` function, after a successful login response is received and `student` is decoded, add the following logic:

```swift
if student.cardStatus == "active" {
    KeychainHelper.shared.delete(key: "isCardLost")
}
```

(Use whatever Keychain deletion API is already used in this file — look for how other keys are deleted in `clearAll()` and use the same pattern. The key string is `"isCardLost"`.)

This ensures: when a student logs in and the server confirms their card status is active, any stale `isCardLost` flag is cleared. If `cardStatus` is nil (old API response) or not "active", the flag is left untouched.

Do NOT change any other logout or login logic.
  </action>
  <verify>
    <automated>cd mobile/ios && xcodebuild -scheme BankongSetonStudent -destination "generic/platform=iOS Simulator" build 2>&1 | grep -E "error:|BUILD SUCCEEDED|BUILD FAILED" | tail -20</automated>
  </verify>
  <done>
    - `clearAll()` no longer deletes `"isCardLost"` from Keychain
    - `login()` deletes `"isCardLost"` when `student.cardStatus == "active"`
    - Build succeeds with no errors
    - No other AuthManager logic changed
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Build must succeed: `xcodebuild -scheme BankongSetonStudent -destination "generic/platform=iOS Simulator" build` → `BUILD SUCCEEDED`
2. `grep -n "fatalError" mobile/ios/BankongSetonStudent/Core/Network/APIClient.swift` → zero results
3. `grep -n "isCardLost" mobile/ios/BankongSetonStudent/Core/Auth/AuthManager.swift` → should show the login clearance line only, NOT in the keysToDelete array
4. `grep -n "cardStatus" mobile/ios/BankongSetonStudent/Models/LoginModels.swift` → shows the new field
</verification>

<success_criteria>
- No `fatalError` calls in APIClient.swift
- `APIError` has `case invalidURL`
- `Student` model has `cardStatus: String?`
- `clearAll()` does NOT touch `"isCardLost"` Keychain key
- `login()` clears `"isCardLost"` when cardStatus is "active"
- iOS project builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/27-critical-mobile-fixes/27-01-SUMMARY.md` using the summary template.
</output>
