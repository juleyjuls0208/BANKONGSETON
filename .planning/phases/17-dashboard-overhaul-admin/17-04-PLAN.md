---
phase: 17-dashboard-overhaul-admin
plan: "04"
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - backend/dashboard/admin_dashboard.py
  - backend/dashboard/templates/students.html
autonomous: true
requirements:
  - DASH-04

must_haves:
  truths:
    - "Admin can enter a student ID and an amount and click 'Top Up' from the Students page"
    - "The top-up correctly adds the amount to the student's balance in Google Sheets"
    - "A transaction record is written to the Transactions Log sheet (Type: Load)"
    - "Non-admin users do not see the top-up form (role check)"
    - "Error messages shown for unknown student ID or amount <= 0"
  artifacts:
    - path: "backend/dashboard/admin_dashboard.py"
      provides: "POST /api/admin/topup endpoint"
      contains: "api/admin/topup"
    - path: "backend/dashboard/templates/students.html"
      provides: "Admin-only top-up modal form"
      contains: "topup-modal"
  key_links:
    - from: "students.html top-up form"
      to: "/api/admin/topup"
      via: "fetch POST with JSON body"
      pattern: "admin/topup"
    - from: "/api/admin/topup"
      to: "Money Accounts sheet"
      via: "get_worksheet_with_retry + update_cell"
      pattern: "Money Accounts"
---

<objective>
Add an admin-only balance top-up feature. The existing `/api/load-balance` takes a physical RFID card number — unusable from a web form. This plan adds `/api/admin/topup` that accepts `student_id` + `amount`, looks up the student's money card internally, and calls the same balance update + transaction-log logic. A Bootstrap modal on `students.html` provides the UI.

Purpose: DASH-04 — admin needs to manually credit a student's balance (e.g., cash payment received).
Output: New backend endpoint + admin-only modal on students page.
</objective>

<execution_context>
@C:/Users/admin/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- EXISTING BALANCE UPDATE LOGIC (admin_dashboard.py ~line 1145) -->
<!--
  /api/load-balance (POST) — takes {money_card, amount}:
  1. Validates amount > 0
  2. get_worksheet_with_retry("Money Accounts") → find row by MoneyCardNumber
  3. current_balance = account["Balance"]
  4. new_balance = current_balance + amount
  5. money_sheet.update_cell(row_index, balance_col, new_balance)
  6. money_sheet.update_cell(row_index, total_col, total_loaded + amount)
  7. money_sheet.update_cell(row_index, update_col, timestamp)
  8. transactions_sheet.append_row([txn_id, timestamp, student_id, money_card,
       "Load", amount, current_balance, new_balance, "Completed", "", ""])
  9. Optionally sends email notification

  NEW ENDPOINT /api/admin/topup (POST) — takes {student_id, amount}:
  Same steps 1-8, but first resolves student_id → money_card via Users sheet.
  Reuse exact same logic — do NOT call load_balance() internally (it re-reads request.json).
  Instead, replicate the balance update block directly in the new route function.
-->

<!-- DECORATORS AVAILABLE -->
<!-- @login_required — checks session['logged_in'] -->
<!-- @admin_only — checks session['role'] == 'admin', redirects to /dashboard if not -->
<!-- Both are defined at line ~298 and ~310 of admin_dashboard.py -->

<!-- HELPER FUNCTIONS AVAILABLE -->
<!-- normalize_card_uid(uid) — strips colons, uppercases, line ~289 -->
<!-- get_worksheet_with_retry(sheet_name, retries=2) — line ~212 -->
<!-- get_philippines_time() — line ~135, returns tz-aware datetime -->

<!-- TRANSACTION ROW FORMAT (11 columns — note: 11th column ItemsJson) -->
<!-- [TransactionID, Timestamp, StudentID, MoneyCardNumber, TransactionType,
      Amount, BalanceBefore, BalanceAfter, Status, ErrorMessage, ItemsJson] -->
<!-- load_balance() appends only 10 columns (omits ItemsJson) — match that pattern -->

<!-- students.html currently extends base.html (after Plan 01).
     Add a floating "Top Up" button visible only to admins ({% if role == 'admin' %}).
     Clicking it opens a Bootstrap modal with student_id input + amount input + submit button. -->

<!-- IMPORTANT: The top-up modal student_id field should be pre-fillable.
     If a student row has a "Top Up" button, clicking it passes the student's ID into the modal.
     The students table is populated via JS from /api/students/search — after render,
     attach click handlers on the Top Up buttons. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /api/admin/topup endpoint</name>
  <files>backend/dashboard/admin_dashboard.py</files>
  <action>
Add the following new route to `admin_dashboard.py`. Insert it immediately after the `load_balance()` function (around line 1283, before the `get_recent_transactions` route):

```python
@app.route("/api/admin/topup", methods=["POST"])
@login_required
@admin_only
def admin_topup():
    """Admin manual balance top-up by student ID"""
    try:
        data = request.get_json()
        student_id = str(data.get("student_id", "")).strip()
        amount = float(data.get("amount", 0))

        if not student_id:
            return jsonify({"error": "student_id is required"}), 400
        if amount <= 0:
            return jsonify({"error": "Amount must be greater than 0"}), 400

        # Resolve student_id -> money_card via Users sheet
        users_sheet = get_worksheet_with_retry("Users")
        users = users_sheet.get_all_records()
        student = None
        for u in users:
            if str(u.get("StudentID", "")).strip() == student_id:
                student = u
                break

        if not student:
            return jsonify({"error": f"Student '{student_id}' not found"}), 404

        money_card = normalize_card_uid(str(student.get("MoneyCardNumber", "")))
        if not money_card:
            return jsonify({"error": "Student has no linked money card"}), 400

        student_name = str(student.get("Name", "Unknown"))

        # Find money account row
        money_sheet = get_worksheet_with_retry("Money Accounts")
        money_records = money_sheet.get_all_records()
        account = None
        row_index = None
        for i, record in enumerate(money_records):
            if normalize_card_uid(str(record.get("MoneyCardNumber", ""))) == money_card:
                account = record
                row_index = i + 2  # +2 for header and 0-index
                break

        if not account:
            return jsonify({"error": "Money account not found for this student"}), 404

        # Update balance
        current_balance = float(account.get("Balance", 0))
        new_balance = current_balance + amount
        timestamp = get_philippines_time().strftime("%Y-%m-%d %H:%M:%S")

        balance_col = money_sheet.find("Balance").col
        money_sheet.update_cell(row_index, balance_col, new_balance)

        total_loaded = float(account.get("TotalLoaded", 0)) + amount
        total_col = money_sheet.find("TotalLoaded").col
        money_sheet.update_cell(row_index, total_col, total_loaded)

        update_col = money_sheet.find("LastUpdated").col
        money_sheet.update_cell(row_index, update_col, timestamp)

        # Log transaction
        try:
            transactions_sheet = get_worksheet_with_retry("Transactions Log")
            transaction_id = f"TXN-{get_philippines_time().strftime('%Y%m%d%H%M%S')}"
            transactions_sheet.append_row([
                transaction_id, timestamp, student_id, money_card,
                "Load", amount, current_balance, new_balance, "Completed", ""
            ])
        except Exception:
            pass  # Balance updated; log failure is non-fatal

        invalidate_cache("Money Accounts")
        invalidate_cache("Transactions Log")

        return jsonify({
            "success": True,
            "student_name": student_name,
            "student_id": student_id,
            "amount": amount,
            "new_balance": new_balance
        }), 200

    except (
        gspread.exceptions.APIError,
        gspread.exceptions.SpreadsheetNotFound,
        gspread.exceptions.WorksheetNotFound,
        ConnectionError,
        TimeoutError,
    ) as e:
        logger.error(f"Google Sheets unavailable in admin_topup: {e}")
        return jsonify({"error": "Service unavailable, please try again"}), 503
    except ValueError:
        return jsonify({"error": "Invalid amount value"}), 400
    except Exception as e:
        logger.error(f"Unexpected error in admin_topup: {e}", exc_info=True)
        return jsonify({"error": "An unexpected error occurred"}), 500
```

Ensure the function is placed after the closing of `load_balance()` and before `get_recent_transactions`. Use the same import/helper style as surrounding routes (no new imports needed — `gspread`, `logger`, `jsonify`, `request` all already imported).
  </action>
  <verify>
    <automated>python -c "
import sys; sys.path.insert(0, 'backend/dashboard'); sys.path.insert(0, 'backend')
src = open('backend/dashboard/admin_dashboard.py').read()
assert 'api/admin/topup' in src, '/api/admin/topup route missing'
assert 'admin_topup' in src, 'admin_topup function missing'
assert '@admin_only' in src.split('admin_topup')[0].split('load_balance')[-1], '@admin_only decorator missing'
print('admin_topup route OK')
"</automated>
  </verify>
  <done>POST /api/admin/topup exists, requires admin session, accepts {student_id, amount}, updates Money Accounts balance, logs a Load transaction, returns {success, new_balance}</done>
</task>

<task type="auto">
  <name>Task 2: Add top-up modal to students.html</name>
  <files>backend/dashboard/templates/students.html</files>
  <action>
In `students.html`, inside `{% block content %}`, make the following additions:

**1. Add a page-level "Top Up Balance" button** in the page header area (visible only to admins). Place it beside or below any existing action buttons at the top of the students content:

```html
{% if role == 'admin' %}
<button class="btn btn-success btn-sm ms-2" data-bs-toggle="modal" data-bs-target="#topupModal">
  <i class="bi bi-plus-circle me-1"></i>Top Up Balance
</button>
{% endif %}
```

**2. Add the Bootstrap modal** at the end of `{% block content %}` (before the closing `{% endblock %}`):

```html
{% if role == 'admin' %}
<!-- Top-Up Balance Modal -->
<div class="modal fade" id="topupModal" tabindex="-1" aria-labelledby="topupModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="topupModalLabel">
          <i class="bi bi-plus-circle me-2"></i>Top Up Student Balance
        </h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <div class="mb-3">
          <label for="topup-student-id" class="form-label">Student ID</label>
          <input type="text" class="form-control" id="topup-student-id"
                 placeholder="e.g. STU-2024-001" autocomplete="off">
        </div>
        <div class="mb-3">
          <label for="topup-amount" class="form-label">Amount (₱)</label>
          <input type="number" class="form-control" id="topup-amount"
                 min="1" step="0.01" placeholder="e.g. 50">
        </div>
        <div id="topup-feedback" class="alert" style="display:none;"></div>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
        <button type="button" class="btn btn-success" id="topup-confirm-btn">
          <span id="topup-btn-text">Confirm Top Up</span>
          <span id="topup-spinner" class="spinner-border spinner-border-sm ms-1" style="display:none;"></span>
        </button>
      </div>
    </div>
  </div>
</div>
{% endif %}
```

**3. Add JS in `{% block extra_js %}`** (append to existing extra_js block if one exists):

```html
{% if role == 'admin' %}
<script>
// Pre-fill student ID when "Top Up" button is clicked from the student table
// Student rows are rendered by existing JS — attach handler after table render
function openTopUpModal(studentId) {
  document.getElementById('topup-student-id').value = studentId || '';
  document.getElementById('topup-amount').value = '';
  document.getElementById('topup-feedback').style.display = 'none';
  var modal = new bootstrap.Modal(document.getElementById('topupModal'));
  modal.show();
}

document.getElementById('topup-confirm-btn').addEventListener('click', async function () {
  const studentId = document.getElementById('topup-student-id').value.trim();
  const amount    = parseFloat(document.getElementById('topup-amount').value);
  const feedback  = document.getElementById('topup-feedback');
  const spinner   = document.getElementById('topup-spinner');
  const btnText   = document.getElementById('topup-btn-text');

  feedback.style.display = 'none';
  if (!studentId || isNaN(amount) || amount <= 0) {
    feedback.className = 'alert alert-warning';
    feedback.textContent = 'Please enter a valid student ID and amount.';
    feedback.style.display = '';
    return;
  }

  spinner.style.display = '';
  btnText.textContent = 'Processing...';
  document.getElementById('topup-confirm-btn').disabled = true;

  try {
    const res = await fetch('/api/admin/topup', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      credentials: 'same-origin',
      body: JSON.stringify({ student_id: studentId, amount: amount })
    });
    const data = await res.json();

    if (res.ok && data.success) {
      feedback.className = 'alert alert-success';
      feedback.textContent = `✓ ₱${amount.toFixed(2)} added to ${data.student_name}. New balance: ₱${parseFloat(data.new_balance).toFixed(2)}`;
      feedback.style.display = '';
      // Refresh student list if the page has a search/reload function
      if (typeof loadStudents === 'function') loadStudents();
    } else {
      feedback.className = 'alert alert-danger';
      feedback.textContent = data.error || 'Top-up failed. Please try again.';
      feedback.style.display = '';
    }
  } catch (e) {
    feedback.className = 'alert alert-danger';
    feedback.textContent = 'Network error. Please check your connection.';
    feedback.style.display = '';
  } finally {
    spinner.style.display = 'none';
    btnText.textContent = 'Confirm Top Up';
    document.getElementById('topup-confirm-btn').disabled = false;
  }
});
</script>
{% endif %}
```

**4.** In the student table JS (wherever students are rendered into table rows), add a "Top Up" button column for admin users. Find the section of JS that builds `<tr>` rows for students and add:

```javascript
// Inside the row-building loop (pseudo-code — adapt to actual template JS):
// Add to the actions cell if role is admin:
`<button class="btn btn-xs btn-outline-success" onclick="openTopUpModal('${student.StudentID}')">
   <i class="bi bi-plus-circle"></i> Top Up
</button>`
```

Note: Inspect the existing students.html JS to find where `<tr>` rows are constructed and insert the button into the actions column (or add an actions column if none exists). Keep all existing buttons/actions intact.
  </action>
  <verify>
    <automated>python -c "
src = open('backend/dashboard/templates/students.html').read()
assert 'topupModal' in src, 'topupModal missing'
assert 'topup-student-id' in src, 'student id input missing'
assert 'admin/topup' in src, 'API endpoint reference missing'
assert 'role == .admin.' in src or \"role == 'admin'\" in src, 'admin role check missing'
print('students.html topup modal OK')
"</automated>
  </verify>
  <done>Admin-only "Top Up Balance" button opens a modal; modal posts to /api/admin/topup; success/error feedback shown inline; student table rows have per-row Top Up buttons that pre-fill the modal</done>
</task>

</tasks>

<verification>
1. `grep "api/admin/topup" backend/dashboard/admin_dashboard.py` → route definition present
2. `grep "topupModal" backend/dashboard/templates/students.html` → modal present
3. `grep "admin_only" backend/dashboard/admin_dashboard.py | grep topup` → decorator applied
4. Jinja2 parse check on students.html passes
</verification>

<success_criteria>
- POST /api/admin/topup exists, decorated with @login_required and @admin_only
- Accepts {student_id, amount}, validates both, resolves card number internally
- Updates Money Accounts balance and writes a Load transaction to Transactions Log
- students.html has admin-only Top Up modal with student ID + amount inputs
- Success response shows new balance; error states shown inline
- No new Python packages; PythonAnywhere compatible
</success_criteria>

<output>
After completion, create `.planning/phases/17-dashboard-overhaul-admin/17-04-SUMMARY.md`
</output>
