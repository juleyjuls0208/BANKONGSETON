---
phase: 17-dashboard-overhaul-admin
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/17-dashboard-overhaul-admin/17-05-PLAN.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - ADM-03
  - ADM-04
gap_closure: true

must_haves:
  truths:
    - "REQUIREMENTS.md has formal ADM-03 and ADM-04 entries for live search and per-student transaction history"
    - "Plan 17-05 frontmatter requirements field references ADM-03 and ADM-04, not ADM-01/ADM-02"
    - "DASH-01 and DASH-05 checkboxes in REQUIREMENTS.md are marked [x] (complete)"
    - "REQUIREMENTS.md traceability table reflects DASH-01 and DASH-05 as Complete"
    - "ROADMAP Phase 17 requirements line includes ADM-03 and ADM-04"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "ADM-03, ADM-04 entries under Admin Features section; updated checkboxes and traceability"
      contains: "ADM-03"
    - path: ".planning/phases/17-dashboard-overhaul-admin/17-05-PLAN.md"
      provides: "Corrected frontmatter with accurate requirement IDs"
      contains: "ADM-03"
  key_links:
    - from: "17-05-PLAN.md frontmatter"
      to: "REQUIREMENTS.md ADM-03/ADM-04 definitions"
      via: "requirements field references"
      pattern: "ADM-03|ADM-04"
---

<objective>
Fix requirement traceability gaps found by Phase 17 verification. All Phase 17 code is correctly implemented and fully working — this is a pure documentation/bookkeeping fix.

**Root cause:** Plan 17-05 was authored with placeholder requirement IDs (ADM-01, ADM-02) that were already claimed by Plans 17-04 and 17-03 respectively. The live student search and per-student transaction history features delivered by 17-05 have no formal requirement IDs in REQUIREMENTS.md.

**Fix:** Register two new requirement IDs — ADM-03 (live student search) and ADM-04 (per-student transaction history) — and align all planning artifacts to reference them.

Purpose: Clean requirement traceability; VERIFICATION.md gap resolved.
Output: Updated REQUIREMENTS.md, corrected 17-05-PLAN.md frontmatter, updated ROADMAP.
</objective>

<execution_context>
@C:/Users/admin/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/17-dashboard-overhaul-admin/17-05-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ADM-03 and ADM-04 to REQUIREMENTS.md; fix DASH-01/DASH-05 status</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Make three targeted edits to REQUIREMENTS.md:

**Edit 1 — Fix DASH-01 checkbox** (line ~27):
Change:
  `- [ ] **DASH-01**: Dashboard uses modern CSS framework (Bootstrap 5 or Tailwind) — full visual redesign`
To:
  `- [x] **DASH-01**: Dashboard uses modern CSS framework (Bootstrap 5 or Tailwind) — full visual redesign`

**Edit 2 — Fix DASH-05 checkbox** (line ~30):
Change:
  `- [ ] **DASH-05**: Dashboard remains deployable on PythonAnywhere free tier (no paid add-ons required)`
To:
  `- [x] **DASH-05**: Dashboard remains deployable on PythonAnywhere free tier (no paid add-ons required)`

**Edit 3 — Append ADM-03 and ADM-04 to Admin Features section** (after ADM-02, before the closing `---`):
Find the Admin Features section which ends with:
  `- [x] **ADM-02**: Admin can export transaction CSV with optional date range filter`
Append immediately after that line:
  `- [x] **ADM-03**: Admin can search students by name or ID with live (debounced) filtering on the students page`
  `- [x] **ADM-04**: Admin can view per-student transaction history in a modal without leaving the students page`

**Edit 4 — Fix traceability table** — find the rows for DASH-01 and DASH-05 (currently "Pending"):
Change `| DASH-01 | Phase 17 - Dashboard Overhaul | Pending |` → `| DASH-01 | Phase 17 - Dashboard Overhaul | Complete |`
Change `| DASH-05 | Phase 17 - Dashboard Overhaul | Pending |` → `| DASH-05 | Phase 17 - Dashboard Overhaul | Complete |`

**Edit 5 — Add ADM-03 and ADM-04 to traceability table** — find the ADM-02 traceability row and append two new rows after it:
```
| ADM-03 | Phase 17 - Dashboard Overhaul | Complete |
| ADM-04 | Phase 17 - Dashboard Overhaul | Complete |
```

**Edit 6 — Update coverage count** — find:
  `- v1.1 requirements: 27 total`
Change to:
  `- v1.1 requirements: 29 total`
Also find `- Unmapped: 0` and leave it as-is (new reqs are mapped).
Also update `- Mapped to phases: 27` → `- Mapped to phases: 29`.
  </action>
  <verify>
    <automated>python -c "
src = open('.planning/REQUIREMENTS.md').read()
assert '[x] **DASH-01**' in src, 'DASH-01 not marked complete'
assert '[x] **DASH-05**' in src, 'DASH-05 not marked complete'
assert 'ADM-03' in src, 'ADM-03 missing'
assert 'ADM-04' in src, 'ADM-04 missing'
assert 'live (debounced) filtering' in src, 'ADM-03 description missing'
assert 'per-student transaction history' in src, 'ADM-04 description missing'
print('REQUIREMENTS.md OK')
"</automated>
  </verify>
  <done>
- DASH-01 and DASH-05 show [x] in checkbox list
- ADM-03 and ADM-04 defined in Admin Features section with [x]
- Traceability table updated: DASH-01, DASH-05 = Complete; ADM-03, ADM-04 rows added
- Coverage count updated to 29
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix 17-05-PLAN.md frontmatter + update ROADMAP Phase 17 requirements line</name>
  <files>
    .planning/phases/17-dashboard-overhaul-admin/17-05-PLAN.md
    .planning/ROADMAP.md
  </files>
  <action>
**Fix 1 — Plan 17-05 frontmatter:**
Open `.planning/phases/17-dashboard-overhaul-admin/17-05-PLAN.md` and find the frontmatter block at the top. Locate:
```yaml
requirements:
  - ADM-01
  - ADM-02
```
Replace with:
```yaml
requirements:
  - ADM-03
  - ADM-04
```

Also update the `<objective>` section's opening line to remove the stale "(ADM-01)" and "(ADM-02)" parenthetical labels:
Find: `live search (ADM-01)` → replace with `live search (ADM-03)`
Find: `transaction history modal per student (ADM-02)` → replace with `transaction history modal per student (ADM-04)`
Find: `<!-- STUDENTS.HTML SEARCH UX (ADM-01):` → replace with `<!-- STUDENTS.HTML SEARCH UX (ADM-03):`
Find: `<!-- TRANSACTION HISTORY MODAL (ADM-02):` → replace with `<!-- TRANSACTION HISTORY MODAL (ADM-04):`

**Fix 2 — ROADMAP Phase 17 requirements line:**
Open `.planning/ROADMAP.md`. Find the Phase 17 section line:
  `**Requirements:** DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, ADM-01, ADM-02`
Replace with:
  `**Requirements:** DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, ADM-01, ADM-02, ADM-03, ADM-04`

Also add a Plans section after the "Estimated plans" bullet list (before the `---` that closes the phase). Append after the last bullet line (`5. (if needed) PythonAnywhere compatibility verification`):

```markdown

**Plans:** 6/6 complete ✓

Plans:
- [x] 17-01-PLAN.md — Bootstrap 5 migration + shared base.html + dashboard.css
- [x] 17-02-PLAN.md — Analytics charts (Chart.js CDN + spending data endpoint)
- [x] 17-03-PLAN.md — CSV export endpoint + transactions page UI
- [x] 17-04-PLAN.md — Admin balance top-up endpoint + modal UI
- [x] 17-05-PLAN.md — Live student search (ADM-03) + per-student transaction history modal (ADM-04)
- [x] 17-06-PLAN.md — Gap closure: requirement ID traceability fixes
```
  </action>
  <verify>
    <automated>python -c "
import re
plan = open('.planning/phases/17-dashboard-overhaul-admin/17-05-PLAN.md').read()
assert 'ADM-03' in plan, '17-05 still references wrong requirement IDs'
assert 'ADM-04' in plan, '17-05 still references wrong requirement IDs'
assert 'ADM-01' not in plan.split('---')[0], 'ADM-01 still in 17-05 frontmatter'

roadmap = open('.planning/ROADMAP.md').read()
phase17_section = roadmap[roadmap.index('## Phase 17:'):]
assert 'ADM-03' in phase17_section, 'ROADMAP Phase 17 missing ADM-03'
assert 'ADM-04' in phase17_section, 'ROADMAP Phase 17 missing ADM-04'
print('17-05-PLAN.md and ROADMAP.md OK')
"</automated>
  </verify>
  <done>
- 17-05-PLAN.md frontmatter requirements field shows ADM-03 and ADM-04 (not ADM-01/ADM-02)
- Inline ADM-01/ADM-02 labels in 17-05 objective and context comments updated
- ROADMAP Phase 17 requirements line includes ADM-03 and ADM-04
- ROADMAP Phase 17 has a Plans completion section listing all 6 plans as [x]
  </done>
</task>

</tasks>

<verification>
1. `python -c "src=open('.planning/REQUIREMENTS.md').read(); assert 'ADM-03' in src and 'ADM-04' in src; print('ADM-03/04 defined')"` → passes
2. `python -c "src=open('.planning/phases/17-dashboard-overhaul-admin/17-05-PLAN.md').read(); front=src.split('---')[1]; assert 'ADM-03' in front and 'ADM-04' in front; print('17-05 frontmatter OK')"` → passes
3. `python -c "src=open('.planning/REQUIREMENTS.md').read(); assert '[x] **DASH-01**' in src and '[x] **DASH-05**' in src; print('checkboxes OK')"` → passes
4. `python -c "src=open('.planning/ROADMAP.md').read(); s=src[src.index('## Phase 17:'):]; assert 'ADM-03' in s; print('ROADMAP OK')"` → passes
</verification>

<success_criteria>
- REQUIREMENTS.md has ADM-03 (live search) and ADM-04 (transaction history modal) with [x]
- DASH-01 and DASH-05 checkboxes corrected to [x] in REQUIREMENTS.md
- Traceability table complete and accurate (DASH-01, DASH-05, ADM-03, ADM-04 all show Complete)
- Plan 17-05 frontmatter references ADM-03 and ADM-04 instead of ADM-01/ADM-02
- ROADMAP Phase 17 requirements line includes all 9 IDs
- All changes are documentation-only — zero code files modified
</success_criteria>

<output>
After completion, create `.planning/phases/17-dashboard-overhaul-admin/17-06-SUMMARY.md`
</output>
