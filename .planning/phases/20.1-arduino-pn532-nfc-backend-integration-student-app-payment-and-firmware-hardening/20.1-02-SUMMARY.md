# Plan 20.1-02 Summary — ArduinoBridge NFC Parsing

## Status: COMPLETE ✓

## What was built

Extended `backend/dashboard/arduino_bridge.py` to handle NFC payment serial lines from the R3 firmware:

- **`_parse_line(line)`** — routes `NFC|<token>`, `ERROR|NFC_FAIL`, and `<CARD|uid>` lines to correct handlers
- **`_post_nfc_payment(token)`** — POSTs to `$API_BASE_URL/api/nfc/pay` with `Authorization: Bearer $CASHIER_JWT` in a daemon thread
- **`_start_serial_thread()`** — extracted for testability; `__init__` unchanged
- `import requests` and `import logging` added; `CASHIER_JWT` / `API_BASE_URL` env vars read at module level with fallback warning

Created `backend/tests/test_arduino_bridge_nfc.py` with 4 pytest tests:
- **BE-01** `test_nfc_payment_event_emitted` — PASS
- **BE-02** `test_nfc_fail_emits_error_result` — PASS
- **BE-03** `test_post_nfc_payment_calls_requests` — PASS
- **BE-04** `test_missing_cashier_jwt_does_not_raise` — PASS

## Commits

- `feat(20.1-02): extend ArduinoBridge with NFC serial parsing and payment POST`

## Verification

```
pytest backend/tests/test_arduino_bridge_nfc.py -v
4 passed in 0.37s
```
