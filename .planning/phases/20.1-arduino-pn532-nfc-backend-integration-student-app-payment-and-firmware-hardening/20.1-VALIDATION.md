---
phase: 20.1
slug: arduino-pn532-nfc-backend-integration-student-app-payment-and-firmware-hardening
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-07
---

# Phase 20.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (existing in `backend/`) |
| **Config file** | `backend/pytest.ini` or inline (no config changes needed) |
| **Quick run command** | `cd backend && pytest tests/test_arduino_bridge_nfc.py -x -q` |
| **Full suite command** | `cd backend && pytest tests/ -q` |
| **Estimated runtime** | ~5 seconds (unit tests only; no hardware required) |

---

## Sampling Rate

- **After every task commit:** Run `cd backend && pytest tests/test_arduino_bridge_nfc.py -x -q`
- **After every plan wave:** Run `cd backend && pytest tests/ -q`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** ~5 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 20.1-01-01 | 01 | 1 | R3-FW-01, R3-FW-02, R3-FW-03 | manual + serial monitor | `cd backend && pytest tests/test_arduino_bridge_nfc.py -x -q` | ❌ W0 | ⬜ pending |
| 20.1-01-02 | 01 | 1 | R4-FW-01, R4-FW-02 | manual + serial monitor | `cd backend && pytest tests/test_arduino_bridge_nfc.py -x -q` | ❌ W0 | ⬜ pending |
| 20.1-02-01 | 02 | 1 | BE-01, BE-02, BE-03, BE-04 | unit (pytest) | `cd backend && pytest tests/test_arduino_bridge_nfc.py -x -q` | ❌ W0 | ⬜ pending |
| 20.1-03-01 | 03 | 2 | UI-01, UI-02, UI-03 | manual browser | manual — see Manual-Only Verifications | n/a | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `backend/tests/test_arduino_bridge_nfc.py` — unit test stubs for BE-01, BE-02, BE-03, BE-04
- [ ] Mock SocketIO fixture (can extend existing test helpers if any)

*Wave 0 must create these before any Wave 1 backend tasks execute.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| R3 firmware emits `NFC|<token>` on HCE phone tap | R3-FW-01 | Requires physical PN532 hardware + phone with BANKONGSETON HCE app | Flash sketch; open Serial Monitor at 9600 baud; tap NFC phone; observe `NFC|...48chars` line |
| R3 firmware falls back to `CARD|<uid>` for physical card | R3-FW-02 | Requires physical MIFARE card + PN532 hardware | Tap physical card; observe `CARD|...` line |
| R3 LCD shows correct states (Tap Phone/Reading/OK/NFC Error) | R3-FW-03 | LCD is visual hardware output | Observe LCD during APDU flow |
| R4 firmware POSTs token to `/api/nfc/pay` | R4-FW-02 | Requires R4 hardware + WiFi + PN532 module | Monitor server logs for POST receipt after phone tap |
| Cashier blue modal appears on `nfc_payment` event | UI-01, UI-02 | Visual browser check | Open cashier page; from browser console: `socket.emit('nfc_payment', {token: 'A'.repeat(48), status: 'pending'})`; verify blue modal |
| Cashier green modal for RFID vs blue for NFC | UI-02 | Visual color distinction | Emit both events; compare modal `background-color` in DevTools |
| Error + retry button on `nfc_error` | UI-03 | Interactive UI element | Emit `socket.emit('nfc_error', {reason: 'nfc_fail'})`; verify retry button in result area |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
