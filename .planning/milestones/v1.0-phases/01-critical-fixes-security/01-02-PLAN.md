---
phase: 01-critical-fixes-security
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/dashboard/cashier/templates/cashier_index.html
autonomous: true
gap_closure: true
requirements:
  - BUG-01

must_haves:
  truths:
    - "Cashier POS page loads without JavaScript parse errors and shows the product grid"
    - "The loadProducts() function fetches from the correct dashboard endpoint (/api/products/list or /cashier/api/products) on the same server"
    - "The socket.io script tag is loaded before any io() initialization code"
    - "There is exactly one complete HTML document in cashier_index.html -- no duplicate script blocks or merged template fragments"
  artifacts:
    - path: "backend/dashboard/cashier/templates/cashier_index.html"
      provides: "Clean cashier POS template with working product loading, cart, WebSocket, and checkout"
      min_lines: 200
  key_links:
    - from: "cashier_index.html loadProducts()"
      to: "dashboard /api/products/list endpoint"
      via: "fetch() call with correct URL"
      pattern: "fetch.*api/products"
    - from: "cashier_index.html initWebSocket()"
      to: "socket.io CDN script"
      via: "io() constructor"
      pattern: "socket\\.io.*\\.js"
---

<objective>
Fix the corrupted cashier POS template that causes a blank screen (BUG-01).

Purpose: The cashier_index.html file contains two merged template versions. Raw HTML tags inside a JavaScript function body cause a parse error that prevents loadProducts() from executing. The cashier POS shows "Loading products..." forever.
Output: A single clean cashier_index.html with working product grid, cart management, WebSocket integration, and checkout flow.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-critical-fixes-security/01-RESEARCH.md
@backend/dashboard/cashier/templates/cashier_index.html
@backend/dashboard/cashier/cashier_routes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite cashier_index.html as a single clean template</name>
  <files>backend/dashboard/cashier/templates/cashier_index.html</files>
  <action>
The current file has THREE problems:
1. Two merged template versions: the first `<script>` block (line 95) has raw HTML tags inside the `renderProducts()` function body starting at line 190, causing a JS parse error.
2. After `</html>` at line 337, there is MORE JavaScript code (a third fragment from yet another merge).
3. The `loadProducts()` function calls `/api/products` which is on port 5001 (mobile API server), but the cashier runs on port 5003 (dashboard server). The dashboard has its own products endpoint.

**Fix:** Rewrite the entire file as a single clean template. Use the SECOND version (lines 214-335) as the base because it has a working loadProducts(), renderProducts(), cart management. Then merge in the FIRST version's WebSocket integration (initWebSocket, checkout with Arduino check, completeSale) which the second version lacks.

Specific requirements:
1. **Single HTML document** -- one `<html>`, one `<head>`, one `<body>`, one `<script>` block. No duplicates.
2. **Correct products endpoint** -- change `fetch('/api/products')` to `fetch('/api/products/list')`. The dashboard app (`admin_dashboard.py`) serves this endpoint at line 270 and returns `{products: [{id, name, category, price, image_url, active, date_added, row}]}`. The cashier template only needs `id`, `name`, `category`, `price` from each product. Filter to only show `active` products in the grid.
3. **socket.io CDN loaded BEFORE script block** -- move `<script src="https://cdn.socket.io/4.5.4/socket.io.min.js"></script>` to BEFORE the main `<script>` block. Currently it is at line 521, after `initWebSocket()` runs, causing `io is not defined`.
4. **WebSocket integration** -- include `initWebSocket()` from the first version (lines 425-441 of the original). It handles `card_read`, `card_timeout`, `card_error` events from the server.
5. **Arduino-aware checkout** -- use the checkout function from the first version's third fragment (lines 443-478) that checks `arduinoConnected` before proceeding, calls `/cashier/api/process-sale`, and shows the payment modal.
6. **completeSale function** -- include from lines 480-510 of the original, which calls `/cashier/api/complete-sale` with the card_uid.
7. **DOMContentLoaded handler** -- use the first version's pattern (line 103-107): call `loadPorts()`, `loadProducts()`, `initWebSocket()` on DOMContentLoaded.
8. **Error handling in loadProducts** -- wrap the fetch in try/catch. Show "Error loading products" message on failure, not a blank screen.
9. **Search bar listener** -- `document.getElementById('searchBar').addEventListener('input', renderProducts)` for live filtering.
10. **Keep all existing CSS** -- preserve the style block from lines 7-48 exactly as-is.
11. **Keep all existing HTML structure** -- preserve header, main-container, products-section, cart-section, and modal from lines 50-93.

The final file should be a clean, working single-page cashier POS with: product grid loading from dashboard API, category filtering, search, cart management (add/remove/qty), Arduino connection, WebSocket card reading, and checkout flow.

Do NOT add error acknowledgment UI yet (that is a user decision for cashier-facing errors but not for product loading). Product loading errors can be simple text messages.
  </action>
  <verify>
    <automated>python -c "html=open('backend/dashboard/cashier/templates/cashier_index.html','r',encoding='utf-8').read(); assert html.count('&lt;html') == 1, 'multiple html tags'; assert html.count('&lt;/html>') == 1, 'multiple closing html tags'; assert '/api/products/list' in html, 'wrong products endpoint'; assert html.index('socket.io.min.js') < html.index('initWebSocket'), 'socket.io loaded after init'; print('Template structure OK')"</automated>
    <manual>Open the cashier page in a browser and verify the product grid loads (requires running server with products data)</manual>
  </verify>
  <done>cashier_index.html is a single clean HTML document with no duplicate elements, loads products from the correct dashboard endpoint, loads socket.io before initialization, and contains complete cart/checkout/WebSocket functionality.</done>
</task>

</tasks>

<verification>
1. `python -c "html=open('backend/dashboard/cashier/templates/cashier_index.html','r',encoding='utf-8').read(); assert html.count('<html') == 1; print('Single HTML doc: OK')"` -- passes
2. `grep -c 'function loadProducts' backend/dashboard/cashier/templates/cashier_index.html` -- returns 1 (exactly one definition)
3. `grep -c 'function renderProducts' backend/dashboard/cashier/templates/cashier_index.html` -- returns 1
4. `grep '/api/products' backend/dashboard/cashier/templates/cashier_index.html` -- shows `/api/products/list`, NOT bare `/api/products`
5. No raw HTML tags appear inside `<script>` blocks
</verification>

<success_criteria>
- Cashier POS template is a single valid HTML document
- JavaScript has no parse errors (no HTML tags inside JS function bodies)
- Products load from the dashboard's own endpoint, not the mobile API
- socket.io is available when initWebSocket() runs
- Cart, checkout, and WebSocket flows are complete and functional
</success_criteria>

<output>
After completion, create `.planning/phases/01-critical-fixes-security/01-02-SUMMARY.md`
</output>
