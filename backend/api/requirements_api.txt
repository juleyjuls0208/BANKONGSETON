# Flask API server
flask==3.0.0
flask-cors==4.0.0

# Environment configuration
python-dotenv>=1.0.0
pytz>=2024.1

# Google Sheets
gspread>=5.12.0

# Firebase Admin (FCM push notifications)
firebase-admin>=6.0.0

# SMS notifications (optional — requires TWILIO_* env vars)
twilio>=9.0.0

# Password hashing for cashier accounts
bcrypt>=4.0.0

# Health monitoring
psutil>=5.9.0

# Production server
gunicorn==21.2.0

# Security
pyjwt==2.8.0
cryptography>=41.0.7   # ponytail: was ==41.0.7; dashboard pins 46.0.5, unify on >= so one venv resolves
